# 03_UNet稳定性 交付范围内正式对象清单

## 先看结论

- source_stage: `03_UNet稳定性`
- source_manifest: `reports/tables/unet_stage_manifest.csv`（总 handoff manifest；`a2_stage_manifest.csv` 仅为局部单 run 汇总）
- source_protocol_version: `eval_proto_v1`
- source_run_name: `A2_UNet_GlaS_seed3407`、`A2_UNet_GlaS_seed1234`、`A2_UNet_GlaS_seed2025`
- consumer_stage: `04_Baseline`
- consumer_file: `结直肠腺体分割_plan_优化版/01_实验执行/04_Baseline/00_阶段总协议.md`
- consumption_boundary: `清单只负责交付对象边界；A2 结果可作 baseline 输入，不表示 B1 已开始`

- 当前纳入对象级说明文映射的 A 类对象共有 `10` 个。
- 当前阶段目标是三 seed 稳定性验证（A2），所以清单核心是三次重复实验的配置、聚合脚本、评估协议和结果资产。
- 当前阶段复用了 02 阶段已解释过的训练主链与数据链，本清单只列出 A2 阶段真正变更或新增的正式对象。
- 当前清单只能说明对象级说明文已建立，不能替代 workflow gate 判定；本轮身份/历史结果审计已收口，当前总 handoff manifest 为 `reports/tables/unet_stage_manifest.csv`。

## 当前纳入范围

### 一、脚本与源码对象 `4` 个

1. `scripts/train.py`
2. `scripts/test.py`
3. `scripts/summarize_stage.py`
4. `src/data/datasets.py`

### 二、核心配置对象 `4` 个

1. `configs/experiment/A2_UNet_GlaS_v1_seed3407.yaml（v1为配置文件版本，当前run_name为A2_UNet_GlaS_seed3407）`
2. `configs/experiment/A2_UNet_GlaS_v1_seed1234.yaml`
3. `configs/experiment/A2_UNet_GlaS_v1_seed2025.yaml`
4. `configs/eval/eval_proto_v1.yaml`

### 三、聚合结果资产 `2` 个

1. `reports/tables/unet_seed_results.csv`
2. `reports/tables/unet_mean_std_summary.csv`

## 当前纳入范围外但已存活的过程证据

下面这些对象是 A2 三 seed 的正式训练产物，本轮未逐个建对象级说明文，但它们真实存活、可审计，是聚合结果表的物理来源：

1. `experiments/A2_UNet_GlaS_seed3407/` 完整训练+评估产物（run_meta.yaml / train_log.csv / val_metrics.csv / checkpoints/best.ckpt / testA_metrics.csv / testB_metrics.csv / metric_crosscheck_note.md / summaries/run_summary.md）
2. `experiments/A2_UNet_GlaS_seed1234/` 同构完整产物
3. `experiments/A2_UNet_GlaS_seed2025/` 同构完整产物
4. `reports/stage_reports/unet_stability_note.md` 与 `reports/stage_reports/unet_stability_stage_summary.md` 稳定性分析与验收结论

## 为什么当前是这 `10` 个

当前这批对象被纳入，是因为它们正好构成 `03_UNet稳定性` 的最小可审计闭环：

1. 三 seed 实验入口配置全部在其中。
2. 训练、评估、聚合三段式实验链的脚本全部在其中。
3. 评估协议冻结来源在其中。
4. 数据协议模块（本轮修复行内注释解析）在其中。
5. 汇总层的 raw seed 表与 mean±std 聚合表在其中；逐 run sample-only CSV、同空间 PNG 和独立复核属于过程证据。

尤其要强调 A2 相对 A1 的口径变化：

1. `scripts/train.py` 的 build_run_meta() 本轮补写了四个协议红线字段（eval_cast_policy / connected_components_connectivity / boundary_metric_impl / connected_components_impl）。
2. `scripts/summarize_stage.py` 本轮扩展了 main_a2()，从 02 阶段的单 seed 汇总升级为三 seed 聚合。
3. `configs/eval/eval_proto_v1.yaml` 本轮补齐了 boundary_metric_impl 与 connected_components_impl，完成 eval config 链落盘。

## A / B / C 边界

当前清单只把 A 类对象放进正式对象范围。

没有纳入 A 类的，主要包括：

1. `.trae/skills/**` 下的规范与示范稿
2. `b_class_auxiliary/**` 下的 guard、gate、runtime_check、diagnostics 留痕
3. 02 阶段已解释、A2 未再变更的源码主链（如 `src/models/unet.py`、`src/losses/seg_losses.py` 等）

这些文件仍然重要，但它们目前不属于本清单的 A2 阶段 A 类正式对象范围。

## 为什么不直接宣称 A2 全量说明文结束

原因不是这 `10` 个对象不够关键，而是 A2 复用了大量 02 阶段已解释的对象，本清单只覆盖 A2 真正变更或新增的部分。

如果后续继续扩范围，仍然要重新完成下面几步：

1. 把新对象纳入清单裁决。
2. 补源码来源 docstring。
3. 补对象级说明文。
4. 同步更新 `README.md` 和 `implementation_status.md`。

所以当前能成立的诚实结论是：

1. 当前锁定范围内的 `10` 个 A 类对象已纳入正式说明文覆盖。
2. 当前三 seed 的聚合说明文已和真实 CSV 数值对齐。
3. 这不等于 A2 阶段全量所有对象都已完成说明文收口。

## 最短阅读顺序

建议按下面顺序读：

1. `README.md`
2. `implementation_status.md`
3. `scripts_summarize_stage.py.md`
4. `reports_tables_unet_mean_std_summary.csv.md`
5. `reports_tables_unet_seed_results.csv.md`
6. `scripts_train.py.md`
7. `scripts_test.py.md`
8. `src_data_datasets.py.md`
9. `configs_eval_eval_proto_v1.yaml.md`
10. 三个 seed 配置说明文按 README 顺序继续阅读

## 当前 A2 正式身份、协议与结果边界

三个正式身份分别为 `A2_UNet_GlaS_seed3407`、`A2_UNet_GlaS_seed1234`、`A2_UNet_GlaS_seed2025`，当前协议为 `eval_proto_v1`。配置文件名中的 `A2_UNet_GlaS_v1_seed*` 仅为配置版本路径说明；`protocol_v3` 仅作历史追溯，不参与当前汇总或 gate。当前聚合主结果必须来自 `reports/tables/unet_mean_std_summary.csv`：testA/testB Object F1=`0.5290508133298323±0.06534870542228736` / `0.5864995222306099±0.017711580461373767`，Object Dice=`0.7081049877960447±0.0528843478663972` / `0.7755628763239749±0.01214631192503348`，Pixel Dice=`0.8687005312137156±0.014245648618802897` / `0.8785019406751632±0.007950925190263055`，IoU=`0.7802676159056027±0.023159000977374777` / `0.7926352354780709±0.009535961930616718`。A2 numbered stage gate=true、workflow_gate_status=pass、handoff_ready_for_b1=true，但不等于 04 自身通过。

## 本轮重写直接依赖的前置文件

`00_总览与规范`、`02_路线与投稿`、`03_文献证据`、本阶段 README、implementation_status、聚合结果说明和脚本说明；用于裁决对象范围、身份、协议、结果与下游边界。

## 代码落地接口

对象清单对应 `scripts/train.py`、`scripts/test.py`、`scripts/summarize_stage.py`、`src/data/datasets.py` 及三份配置和两张结果表。`run_stage02_training()` 若出现，仅是源码历史函数名，当前消费阶段为 A2。

## 冲突裁决记录

旧高 F1、旧协议和旧身份不进入当前对象清单消费；配置路径中的 v1 保留为合法版本说明。

## 回退条件

若对象资产、协议、结果表或 gate/handoff 失真，清单不得作为放行依据，应回退 blocked 并复核资产。

## 文件质量自检

- [x] 10 个 A 类对象与三 seed 正式身份边界明确。
- [x] 当前协议、精确聚合值、历史项限制和 04 边界明确。
- [x] 上游、接口、冲突和回退均有独立落点。

## Diagnostics 闭环

已扫描本阶段所有当前可见 Markdown；旧身份/协议/数字均已处于配置版本或历史更正语境，未发现未标注当前消费项。

## 审计对表

对象清单 → 范围与身份；README/implementation_status → 当前结论；聚合表 → 精确数字；全目录扫描 → diagnostics；缺口：无。

## 当前真实边界

- 当前已经解释清楚的是：三 seed 实验入口、三段式实验链脚本、评估协议、数据协议模块、三层落盘的聚合结果表。
- 当前还没有逐个建说明文的是：三个 seed 实验目录下的逐文件产物（它们真实存活，由聚合表间接覆盖）。
- 当前最重要的修正点已经完成：A2 的协议红线字段三层落盘已闭合，三 seed 协议一致性校验通过。
