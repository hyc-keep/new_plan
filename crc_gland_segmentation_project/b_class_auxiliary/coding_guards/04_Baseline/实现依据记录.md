# 04_Baseline 实现依据记录

## 2026-07-23 严格复现审查整改

- `src/models/resnet34_unet.py`：B1 继续使用 ImageNet1K V1 的 ResNet34 encoder，但改为读取配置指定的本地权重；当前真实文件为 `/home/featurize/.cache/torch/hub/checkpoints/resnet34-b627a593.pth`，SHA256=`b627a593bcbe140c234610266fe4f8ae95ea42fc881d091c9b6052e6b1d0590f`。路径缺失、文件格式错误或 hash 不匹配直接阻断。
- `configs/model/resnet34_unet.yaml`：冻结 `pretrained_weights_path` 与 `pretrained_weights_sha256`，未改变 B1 模型科学边界。
- `scripts/train.py`、`scripts/test.py`、`scripts/run_baseline_sequential.sh`：PYTHONHASHSEED 改为启动前由 launcher 设置并严格核对；测试 checkpoint 限定当前 run checkpoints，并核对身份字段、best path/hash。
- `scripts/run_baseline_sequential.sh`：完整性必须同时具备 finite train/val/checkpoints、run identity/hash、TestA=60/TestB=20、metric_crosscheck pass、predictions/visuals 等资产；未测试目录不得 SKIP COMPLETE。
- `reproducibility_audit/compare_runs.py`：严格比较环境/配置/source/model/权重 metadata、CSV 和 checkpoint SHA256；允许字段白名单仅覆盖 run_name、seed、时间和路径等可变字段，任一差异返回非零并输出详情。

本文件是 `reports/stage_reports/implementation_tracking/04_Baseline/实现依据记录.md` 的根路径同步副本。

## 当前轮次

- 阶段：`04_Baseline`
- 当前轮次：`fresh_original_protocol_reproduction`
- 状态：`blocked_before_training`
- 目标：A2 UNet 与 B1 ResNet34-UNet 按原始协议成对重训。
- 原则：A2/B1 共享 `train_proto_v1/eval_proto_v1`；B1 只改变 ResNet34 encoder；旧 v1/v2/v3 结果仅作历史证据，不进入当前 Gate。

## 本轮冻结参数

GlaS `train68/val17/TestA60/TestB20`，RGB 512x512，ImageNet mean/std，`light_aug_v1`，BCE+Dice，AdamW，`lr=1e-3`，`weight_decay=1e-4`，ReduceLROnPlateau 监控 `val_objdice`，最多 120 epochs，early stopping 20，batch size 2，AMP，best selector 为 `val_objdice` 最大值，threshold 0.5，TestA/TestB 分开，seeds `3407/1234/2025`。

## 本轮正式文件回链

- 六份 `configs/experiment/A2_UNet_GlaS_seed*.yaml` / `B1_ResNet34_UNet_GlaS_seed*.yaml`：统一原始协议身份，并将 `config_refs.train` 指向 `configs/train/unet_flow_v1.yaml`。
- `configs/train/unet_flow_v1.yaml`：原始训练协议唯一入口。
- `configs/eval/eval_proto_v1.yaml`：固定评估口径。
- `b_class_auxiliary/coding_guards/04_Baseline/stage_contract.yaml`：锁定六个正式 run、schema、lineage、历史排除边界。
- `scripts/run_baseline_sequential.sh`：只接受当前原始协议身份的完整 run，避免旧 v3 资产被误跳过。
- 本文件及阶段归档版：记录依据，不替代 runtime、code quality、workflow gate 或实验结果。

## 诚实边界

当前尚未完成正式训练、测试、独立指标复核或本轮 Gate。不得引用任何预期指标。正式检查必须在本轮配置和契约冻结后重新运行；旧 v3 证据因配置身份已变化而失效，不能复用为当前轮通过证据。
