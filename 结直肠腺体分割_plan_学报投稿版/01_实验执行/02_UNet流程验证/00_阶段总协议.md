# 02_UNet流程验证：阶段总协议

## 文档身份与当前状态
- 文档类型：active route 的正式阶段协议；active plan root 固定为 `/home/featurize/work/Paper/结直肠腺体分割_plan_学报投稿版`。
- active project root：`/home/featurize/work/Paper/crc_gland_segmentation_project_journal`。
- 阶段角色：流程验证/最小基线；UNet 不是当前主方法。N1/N2/N3 是 `05_方法协议` 内的方法身份，不是编号阶段；旧 LKMA/Boundary/Distance 仅作 historical provenance。
- 当前状态：`planned/not_run`；`formal_result=not_run`；`result_eligibility=false`。本文件不声明任何实验已完成。
- 当前 route：`01 数据协议 -> 02 UNet流程验证 -> 03 UNet稳定性 -> 04 Baseline -> 05 方法协议 -> 06 val筛查 -> 07 正式GlaS -> 08 CRAG验证 -> 09 外部对比 -> 10 结果汇总 -> 11 总验收与止损`。

## 本轮重写直接依赖的前置文件
- active plan 的 `00_总览与规范/00-07`：路线、工程映射、参数、命名、评估、证据化写作和质检规则。
- active plan 的 `01_数据协议`：GlaS split、标签、盲态和 `data_stage_pass/handoff_ready/preflight_pass` 门控。
- active plan 的 `02_路线与投稿/*`：当前路线和投稿止损边界。
- active plan 的 `03_文献证据/01_经典基线与对比方法/02_U-Net.md`、`05_腺体任务经典论文/01_GlaS-Challenge.md`、`07_综述与写作支撑/02_Loss-Survey.md`：结构、benchmark 和 BCE+Dice 依据。
- 同阶段强模板：本目录的 `01_任务与默认配置.md`、`02_训练步骤.md`、`03_验证测试与可视化.md`、`05_阶段验收.md`。

## 上游、同层与下游
- 上游：`01_数据协议`，只提供数据身份和放行条件；未真实通过前不可训练。
- 同层：本目录 `01-05`，分别定义配置、训练、评估、排查和验收；不得相互重开冻结口径。
- 下游：`03_UNet稳定性`、`04_Baseline`、`05_方法协议`。下游只能消费已验收协议，不能把本阶段未运行状态改写成结果。
- 历史 provenance：旧计划/旧工程只可作为差异裁决背景，不能作为 current 入口、Gate、上游或下游。

## 路线依据
UNet 只用于证明最小训练与评估链可消费 GlaS 数据。当前正式主线从 `05_方法协议` 的 N1/N2/N3 展开；本阶段不提前打开 N1/N2/N3，也不恢复旧 LKMA/Boundary/Distance 顺序。

## 文献依据
U-Net 文献支持 encoder-decoder 与 skip connection；GlaS 文献支持 `train68/val17/TestA60/TestB20`、TestA/TestB 盲态及对象级评价背景；loss survey 支持 BCE+Dice 作为可解释的工程起点。文献数字不是本项目结果。

## 固定/可调/禁止参数
- 固定：UNet；`512x512`；RGB；ImageNet mean/std；`mask>0`；BCE+Dice；AdamW；正式 seeds `3407/1234/2025` 供后续稳定性协议使用；best checkpoint=`argmax ObjDice on val17`；threshold 只能来自 val17。
- 规划默认：`epoch_max=120`、`batch_size=planned`、`lr=1e-3`、`weight_decay=1e-4`、`early_stopping=20`、AMP on；若 active config 未真实接入，标 `planned/frozen_pending_implementation`。
- 可调：仅允许在 preflight 前登记并升级协议版本的执行预算；不得用 TestA/TestB 反馈。
- 禁止：smoke cap 泄漏到 formal；并发写同一 run 目录；把 N1/N2/N3 当编号阶段；把旧路线当 current；训练、验证、正式 TestA/TestB 混为一个动作。

## 输入/输出资产、schema 与 handoff
- 输入资产：数据协议交接包、train68/val17 训练验证清单；TestA60/TestB20 盲态，仅在冻结 checkpoint/threshold 后消费。
- 规划输出目录：`/home/featurize/work/Paper/crc_gland_segmentation_project_journal/experiments/<run_name>/`。
- schema：`run_meta.yaml` 必须有 `run_name/stage/status/planned_or_actual/formal_result/result_eligibility/dataset/split/blind_state/decision/rollback_target`、config/code/data digest、checkpoint sha256、metric identity 及七字段 lineage；`epoch_metrics.csv` 每 epoch 一行，train/val 成对、`batches` 为真实计数。
- 正式闭环：`train -> val -> checkpoint selector -> TestA/TestB -> raw sample-level metrics -> aggregate -> handoff`；raw 与 aggregate 分离。当前 `checkpoint_selector.py`、正式 `run_eval.py`、完整 object metrics、正式 CSV/manifest 为 `planned_not_created/blocked`。
- handoff 七字段：`source_stage/source_manifest/source_protocol_version/source_run_name/consumer_stage/consumer_file/consumption_boundary`；当前 handoff `blocked`。

## 代码落地接口
- 已知代码落点：`scripts/train.py`、`scripts/test.py`、`scripts/summarize_stage.py`、`src/engine/trainer.py`、`src/data/datasets.py`，状态仅为 `present_unproven`，不等于正式结果链已通过。
- 规划接口：`run_smoke_check()`、`run_runtime_probe()`、`fit_formal_unet()`、`select_best_checkpoint()`、`run_test_split()`、`aggregate_metrics()`、`write_handoff_manifest()`。
- `smoke`、runtime probe、formal training、formal validation、formal TestA/TestB 必须使用不同状态/目录和字段；smoke batch cap 只存在于 smoke 配置。

## 运行记录字段与 lineage
- 必记：`run_meta`、`config_digest`、`code_digest`、`data_digest`、`checkpoint_sha256`、`metric_identity`、`blind_state`、`decision`、`rollback_target`、`formal_result`、`result_eligibility`。
- 当前不得填写真实 digest/hash/metrics；统一为 `not_run/not_created/blocked`。
- 并发写同 run 目录判污染，必须 `decision=rollback`。

## 验收
- 文档 Gate：可检查本协议结构和字段。
- 真实数据 Gate：`blocked`，因数据门控尚未真实通过的证据未提供。
- 代码质量 Gate：`blocked`，脚本存在不等于接口消费闭环成立。
- runtime、formal training/eval/test、结果资格：`not_run/blocked`。
- 只有上述门控和六份文档均证据化后，才可 handoff 到 `03_UNet稳定性`；不得因脚本存在或 py_compile 通过宣称阶段通过。

## 独立回退条件
任一 split/盲态污染、门控缺失、selector/threshold 来源不明、raw/aggregate 混写、真实 batches 缺失、digest/hash/metrics 被伪填、并发写 run 目录或 planned 接口被写成完成，均 `stage_pass=false`，`rollback_target=01_数据协议` 或对应最近已证实层级，禁止继续实验。

## 冲突裁决记录
- 裁决：active plan/project 优先；旧计划/旧工程只保留 provenance，不得作为 current 入口。
- 裁决：当前 route 固定为 01-11；N1/N2/N3 归属 05；UNet 不是主方法。
- 裁决：旧文档中的真实运行叙述、旧 stage 编号、旧工程路径和已完成 metrics 全部降级为历史或删除。
- 修复状态：本六文件批次重写中；代码、数据、结果未修改。

## 文件质量自检
- [ ] 已读 active `00_总览与规范/00-07`，并把路线、参数、命名、评估、代码映射和质检落到正文。
- [ ] 已读 active `01_数据协议`、`02_路线与投稿`、相关 `03_文献证据`，未将历史文件作为 current 依赖。
- [ ] 已写独立身份、依赖、上下游、路线、文献、参数、接口、记录、验收、回退、冲突、Diagnostics、审计对表。
- [ ] 已区分 smoke/runtime/formal，已区分 raw/aggregate，已声明 planned/not_run 与 blocked。
- [ ] 对照模板：本目录 `01/02/03/05`；缩水项：旧文档的历史结果与旧入口；补齐项：active 根、route、lineage、digest、盲态和真实状态。

## Diagnostics 闭环
本轮将对六个目标 Markdown 做 IDE diagnostics 和 checker 双重检查。当前已知诊断为无新增 IDE 问题；checker 未运行前不宣称 Gate pass。若 checker 报 partial，先修正文再复跑。

## 审计对表
| 已读文件 | 正文落点 | 自检落点 | diagnostics | 当前缺口 | 修复状态 | 对照模板 | 缩水项/补齐项 |
|---|---|---|---|---|---|---|---|
| active `00_总览与规范/00-07` | 路线、参数、命名、评估、接口 | 文件质量自检 | 待 checker | checker 待跑 | 修补中 | 同阶段 01/02/03/05 | 补齐 active 根与状态 |
| active `01_数据协议` | 输入、门控、blind_state | 文件质量自检 | 待 checker | data gate 未真实通过 | blocked | 同阶段 05 | 删除旧工程入口 |
| active `02_路线与投稿/*` | route、UNet 角色、N1-N3 归属 | 文件质量自检 | 待 checker | 正式结果未运行 | planned | 同阶段 00 | 补齐 01-11 route |
| active `03_文献证据/*` | U-Net/GlaS/loss 依据 | 文件质量自检 | 待 checker | 文献不等于本项目结果 | planned | 同阶段 01 | 明确 provenance |
| active project code | 代码落地接口 | 文件质量自检 | 待 checker | selector/eval/object metrics/CSV planned | blocked | 同阶段 02/03 | 不把脚本存在写成完成 |
| 当前运行资产 | 运行记录与验收 | 文件质量自检 | 待 checker | digest/hash/metrics 未产生 | not_run | 同阶段 05 | 明确未启动实验 |

## 当前结论
本文档 Gate 可检查；真实数据、代码质量、runtime、formal training/eval/test 和结果资格均 `blocked/not_run`。