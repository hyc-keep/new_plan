# 01-04 全链复现准备实现依据记录

- 文件归类：B 类工程准备、审计与检查对象。
- 状态：`pre-run only / pending_not_run`。
- 说明：以下全部为工程冻结与可审计准备决定，不是论文直接结论，不包含未来实验数值或通过结论。

| 正式文件 | 归类 | 来源与约束 | 工程落点 |
| --- | --- | --- | --- |
| `b_class_auxiliary/tools/check_full_chain_reproduction_contract.py` | B | 本次审计要求机器核验身份、输出新鲜度、四 hash、lineage 与历史排除；参考 `scripts/train.py` 的 hash 路径算法 | 生成 01 freeze manifest，写专用 pre-run report；拒绝已有批次根或任何未落在契约批次根下的 run 输出 |
| `b_class_auxiliary/coding_guards/01_04_full_chain_reproduction/full_chain_contract.yaml` | B | 本次审计事实及统一七字段 lineage 规则 | 锁定 01→04 输入、输出、禁止边界与 pending manifest；新链批次根固定为 `experiments/reproduction_20260724/` |
| `b_class_auxiliary/coding_guards/01_04_full_chain_reproduction/研究定标记录.md` | B | 本次审计事实、`configs/data/glas.yaml`、`scripts/train.py` | 记录已读依据与工程/论文边界 |
| `b_class_auxiliary/coding_guards/01_04_full_chain_reproduction/阶段边界_执行顺序_审计记录.md` | B | 本次审计事实与项目跨阶段消费规则 | 固定 01→02→03→04 的唯一顺序 |
| `configs/experiment/full_chain_reproduction/A1_UNet_GlaS_seed3407_chain.yaml` | B | 现有可靠 v1 A1 配置字段模式与 `scripts/train.py` 配置加载要求 | 新 02 A1 身份和独立输出 |
| `configs/experiment/full_chain_reproduction/A2_UNet_GlaS_seed3407_chain.yaml` | B | 现有可靠 v1 A2 配置字段模式与三 seed 约束 | 新 03 seed 3407 身份 |
| `configs/experiment/full_chain_reproduction/A2_UNet_GlaS_seed1234_chain.yaml` | B | 同上 | 新 03 seed 1234 身份 |
| `configs/experiment/full_chain_reproduction/A2_UNet_GlaS_seed2025_chain.yaml` | B | 同上 | 新 03 seed 2025 身份 |
| `configs/experiment/full_chain_reproduction/B1_ResNet34_UNet_GlaS_seed3407_chain.yaml` | B | 现有可靠 v1 B1 配置字段模式；本次审计的 03→04 只消费新 A2 规则 | 新 04 seed 3407 身份和 `source_a2_manifest` |
| `configs/experiment/full_chain_reproduction/B1_ResNet34_UNet_GlaS_seed1234_chain.yaml` | B | 同上 | 新 04 seed 1234 身份和 `source_a2_manifest` |
| `configs/experiment/full_chain_reproduction/B1_ResNet34_UNet_GlaS_seed2025_chain.yaml` | B | 同上 | 新 04 seed 2025 身份和 `source_a2_manifest` |
| `scripts/train.py` | A | 训练入口必须保证历史配置输出语义不变，并为新链物理隔离训练、smoke 和 runtime probe | 可选 `experiment_root` 受限于项目根目录；新链输出为 `experiments/reproduction_20260724/<run_name>/`；该批次已有 fresh 输出即拒绝覆盖，重试必须建立新批次；旧配置仍默认 `experiments/<run_name>/` |
| `scripts/test.py` | A | 测试入口必须与训练入口解析同一 run 目录，避免训练/测试跨根目录 | 通过 config 的 `experiment_root` 调用训练入口的输出解析函数 |
| `b_class_auxiliary/coding_guards/01_04_full_chain_reproduction/01_data_freeze_manifest.json` | B | 从当前实际 data config、split、源 asset manifest 和引用文件自动生成，禁止手填 hash | 新链唯一 01 数据冻结输入 |
| `b_class_auxiliary/coding_guards/01_04_full_chain_reproduction/pre_run_check_report.md` | B | 检查器真实输出 | 仅表示预运行静态检查，不表示训练或测试通过 |
| `b_class_auxiliary/coding_guards/01_04_full_chain_reproduction/复现执行手册与状态记录.md` | B | 用户要求集中记录 01→04 的真实复现顺序、执行边界和阶段后同步规则；以全链契约、freeze manifest、run 产物与 Gate 为唯一事实输入 | 统一说明 01 未跳过，并为每个阶段保留 pending/pass/fail/blocker 的路径化状态记录；不承载或手填实验数值 |

未修改任何 `experiments/**` 历史结果、CSV、checkpoint 或 `run_meta`。
