# 01–04 复现执行记录

## 1. 文档用途

本文件是 01–04 阶段复现工作的持续记录。每完成一次阶段级验证或独立重复运行，都必须在这里追加一条记录，不覆盖已有记录。

复现结果和运行资产统一写入：

```text
experiments/reproducibility_audit/
```

其中：

```text
01_data_protocol/       01 数据协议验证证据
02_unet_flow/           02 UNet 流程验证证据
03_unet_stability/      03 UNet 三 seed 稳定性复现证据
04_baseline/            04 A2/B1 baseline 验证证据
repeat_runs/            独立 train→test 重复运行资产
reports/                汇总矩阵、差异报告和最终裁决
```

不得覆盖以下正式结果目录：

```text
experiments/A2_UNet_GlaS_seed3407/
experiments/A2_UNet_GlaS_seed1234/
experiments/A2_UNet_GlaS_seed2025/
experiments/B1_ResNet34_UNet_GlaS_seed3407/
experiments/B1_ResNet34_UNet_GlaS_seed1234/
experiments/B1_ResNet34_UNet_GlaS_seed2025/
```

## 2. GitHub 版本基线

审查记录：当前版本 `dcd8cf0` 已发现复现闭环阻断；修复后必须使用新 commit 重新执行 smoke/repeat。GitHub 上传记录保留，不把旧 commit 的通过状态继承到当前轮次。

本轮严格复现闭环代码修复完成后，必须先产生新的 Git commit，再重新执行 smoke/repeat；旧 commit 的 smoke/repeat、比较结果和 Gate 状态一律不得继承。当前修复不启动完整训练。

当前代码、配置、计划、论文材料和复现工具已经上传到 GitHub：

```text
repository: https://github.com/hyc-keep/new_plan.git
branch: main
commit: a1fdff78211de668abd32a21b705e6eca66b6634
short_commit: a1fdff7
```

本次上传的仓库边界：

- 上传实验计划、阶段路线、论文材料、参数依据和说明文档；
- 上传 `src/`、`scripts/`、`configs/`、`b_class_auxiliary/` 和复现审计工具；
- 上传轻量级 YAML、CSV、Markdown 元数据和报告；
- 保留 B1 复现所需的 ResNet34 权重；
- 不上传数据集、checkpoint、预测图、可视化、日志、环境目录和临时历史归档。

ResNet34 预训练权重：

```text
path: crc_gland_segmentation_project/weights/resnet34-b627a593.pth
sha256: b627a593bcbe140c234610266fe4f8ae95ea42fc881d091c9b6052e6b1d0590f
```

GitHub 提交是当前源码和配置的版本锚点，但不等于 01–04 已经完成数值复现认证。正式复现仍必须在当前工作区生成独立运行资产并完成比较。

## 3. 当前冻结协议

当前复现不得修改参数、模型结构或评估规则：

```text
train_proto_v1
eval_proto_v1
lr=1e-3
weight_decay=1e-4
batch_size=2
AMP=true
BCE+Dice
ReduceLROnPlateau monitor=val_objdice
max_epoch=120
early_stopping_patience=20
best_selector=val_objdice_max
threshold=0.5 source=val17
TestA/TestB separate
seeds=3407/1234/2025
```

模型边界：

```text
A2: plain UNet
B1: ResNet34 encoder + U-Net decoder
```

当前 baseline 不启用：

```text
BN policy
差分学习率
LKMA
Boundary head
Distance head
TTA
额外后处理
```

## 4. 复现认证顺序

01–04 都属于复现范围，按以下顺序记录：

```text
01 数据协议
  ↓
02 UNet 流程
  ↓
03 UNet 稳定性（三个 seed）
  ↓
04 A2/B1 baseline（独立 train→test 重复）
  ↓
最终逐项比较和阶段裁决
```

### 4.1 01 数据协议

必须验证：

- 数据目录和文件清单；
- train68、val17、TestA60、TestB20 split；
- 图像和掩膜文件内容 hash；
- manifest、split CSV、数据配置 hash；
- RGB、512×512、归一化和标签编码规则。

状态：`待完成`

### 4.2 02 UNet 流程

必须验证：

- A2 模型构建和初始 state hash；
- forward、loss、backward 和 optimizer 流程；
- DataLoader 顺序和 generator；
- checkpoint 保存和加载；
- validation、best checkpoint 和 test 入口；
- freeze smoke 输出和 finite 检查。

状态：`freeze smoke 已通过；stage-level 正式记录待完成`

### 4.3 03 UNet 稳定性

必须使用 seed `3407/1234/2025` 验证：

- 每个 seed 的训练和验证日志；
- TestA/TestB 指标；
- mean、population std（`ddof=0`）；
- 预注册 stability Gate；
- 当前冻结代码和环境下的结果一致性。

状态：`现有正式结果保留；冻结后完整重复待完成`

### 4.4 04 Baseline

必须验证：

- A2 与 B1 只改变 encoder 结构；
- 训练和评估协议完全一致；
- A2/B1 各完成至少两次同 seed 的完整 train→test；
- TestA/TestB 指标、日志和 checkpoint 状态逐项比较；
- `compare_runs.py` 输出 `REPRODUCIBILITY_AUDIT=PASS`。

状态：`正式双重复尚未完成`

## 5. 单次复现记录模板

每次完成验证后复制本节并填写，不修改历史记录。

### 记录 N：<阶段>/<run 名称>

```text
日期：YYYY-MM-DD
阶段：01 / 02 / 03 / 04
模型：A2 / B1 / 不适用
seed：
run 目录：
启动命令：
Git commit：
source_tree_sha256：
设备：
GPU：
```

固定项检查：

```text
[ ] PYTHONHASHSEED 正确且在 Python 启动前设置
[ ] CUBLAS_WORKSPACE_CONFIG 正确且在 torch 导入前设置
[ ] cudnn.deterministic=true
[ ] cudnn.benchmark=false
[ ] torch.use_deterministic_algorithms=true
[ ] DataLoader generator/worker 设置正确
[ ] 数据、split、manifest hash 已记录
[ ] 配置 hash 已记录
[ ] 模型初始 state hash 已记录
[ ] 预训练权重 hash 已记录（B1）
[ ] AMP 和 GradScaler 参数已记录
[ ] 环境、GPU、CUDA/cuDNN 已记录
```

结果：

```text
状态：PASS / FAIL / BLOCKED
REPRODUCIBILITY_AUDIT：PASS / FAIL / 未执行
主要产物：
差异报告：
阻断原因：
下一步：
```

## 6. 当前记录

### 记录 1：冻结机制和 GitHub 基线

```text
日期：2026-07-23
阶段：01–04 冻结审查
状态：已完成
```

已完成：

- 随机源、deterministic backend、DataLoader、AMP 和 checkpoint 状态机制已固定；
- 数据配置、split、manifest 和实际数据文件 hash 已纳入 run metadata；
- A2/B1 freeze smoke 已通过；
- 严格比较工具已具备缺失即失败和差异非零退出机制；
- GitHub 基线已上传到 `hyc-keep/new_plan` 的 `main` 分支；
- 当前版本 commit 为 `a1fdff7`。

尚未完成：

- 01 stage-level 完整数据协议验证；
- 02 stage-level 完整流程复现记录；
- 03 三 seed 稳定性复现；
- 04 A2/B1 独立完整 train→test 双重复；
- 01–04 最终复现矩阵和 handoff 裁决。

## 7. 最终裁决规则

在以下条件全部满足前，不得宣布 01–04 严格复现完成，也不得进入 05：

1. 01 数据协议验证通过；
2. 02 UNet 流程验证通过；
3. 03 三个 seed 的稳定性实验完成并通过预注册规则；
4. 04 A2/B1 独立 train→test 重复完成；
5. 两次运行的 metadata、CSV、模型状态、optimizer、scheduler、scaler 和 RNG 状态通过严格比较；
6. 运行环境和数据 hash 一致；
7. 所有差异都有明确、可解释、非科学结果层面的原因；
8. `reports/` 中形成最终矩阵和裁决记录。
