# 项目问题与决策登记

## 0. 文件定位

- 文件类别：B 类流程审计 / 问题登记 / 决策记录。
- 文件角色：记录已确认的问题、影响边界、继续条件、回退条件和论文表述边界；不生成实验数值，也不覆盖任何 Gate。
- 当前编号阶段：`04_Baseline`。
- 当前 Gate 对象：`original_protocol_reproduction` 的 six v1 contract runs。
- 正式数值唯一真源：六个 contract run 的逐样本 CSV、`run_meta.yaml`、checkpoint，以及由脚本生成的派生表。
- 当前编号阶段状态唯一真源：`reports/stage_reports/baseline_stage_summary.md`；当前工作流状态唯一真源：`b_class_auxiliary/coding_guards/04_Baseline/workflow_gate_report.md`。
- 历史归档边界：`_historical_archive/` 及其 `archive_manifest.md` 仅供追溯，必须标记为 historical/non-current，不能参与 current v1 Gate、current summary、current manifest 或论文当前结果。

## 1. 当前总状态：v1 current gate

| 层级 | 当前状态 | 依据与边界 |
|---|---|---|
| 当前结果身份 | `original_protocol_reproduction / train_proto_v1 / eval_proto_v1` | 仅消费 `stage_contract.yaml` 锁定的 A2/B1 六个 v1 original-protocol reproduction run。 |
| 正式训练、测试与逐样本资产 | `complete` | 当前 summary 的 `complete_runs=true`；具体资产仍以六个 run 的原始文件为准。 |
| 主要指标方向 | `pass` | 当前自动生成 summary 的 `main_metrics_not_worse=true`。 |
| 稳定性比较 | `pass` | 当前自动生成 summary 的 `stability_not_weaker=true`。 |
| B1 Gate | `pass` | 当前自动生成 summary 的 `gate_b1=true`、`stage_pass_b1=true`。 |
| C1 handoff | `ready` | 当前自动生成 summary 的 `handoff_ready_for_c1=true`。 |
| 04 workflow gate | `pass` | 本轮已实际运行 `enforce_workflow_gate.py` 并重生成 `b_class_auxiliary/coding_guards/04_Baseline/workflow_gate_report.md`；报告结论为 `workflow_gate_status=pass`，且 runtime 与 code-quality 均为 `pass`。 |
| 历史 v3 warning / old Gate | `historical_non_current` | 仅保留为污染排查与决策溯源事实，不能覆盖或降级上述 current v1 结论。 |

## 2. 当前依据与消费边界

| 文件 | 作用 | current v1 结论 |
|---|---|---|
| `b_class_auxiliary/coding_guards/04_Baseline/stage_contract.yaml` | 锁定 six v1 run 的 identity、schema、lineage 与 freshness | current Gate 只允许消费此 contract 指定的 run。 |
| `scripts/summarize_stage.py` | 从 contract run 的 sample rows 自动生成 summary、派生表与 manifest | 不消费 legacy 或 v3 aggregate。 |
| `reports/stage_reports/baseline_stage_summary.md` | 当前编号阶段汇总 | `complete_runs=true`、`main_metrics_not_worse=true`、`stability_not_weaker=true`、`gate_b1=true`、`stage_pass_b1=true`、`handoff_ready_for_c1=true`。 |
| `b_class_auxiliary/coding_guards/04_Baseline/workflow_gate_report.md` | 当前工作流总放行 | 只接受脚本重生成的结论。 |
| 对应 `_historical_archive/` | 旧 v3 identity contamination 派生表、summary 和旧结论 | historical/non-current；`valid_for_current_gate=false`。 |

## 3. 问题登记总表

| 编号 | 问题 | 类型 | 严重度 | 当前状态 | 是否阻断 current v1 handoff | 处理决策 |
|---|---|---|---|---|---|---|
| `P04-001` | 历史 v3 派生结论曾报告 TestB Object Dice stability warning 与旧 Gate 失败 | 历史稳定性 / 旧 Gate | 中 | 已归入 historical/non-current | 否 | 保留该历史事实及其溯源，但不得把它写成 current v1 的 warning、`original_gate_b1=false` 或 handoff blocker。 |
| `P04-002` | 三个 seed 的统计推断范围有限 | 统计边界 | 低到中 | 已知限制 | 否 | current v1 仍逐 seed 报告；不得把有限重复数扩写为显著性或未来复现保证。 |
| `P04-003` | 历史条件性下游研究与 formal handoff 曾混层 | 流程治理 | 中 | 已修订 | 否 | current handoff 只由 current v1 summary 和 workflow gate 决定；历史条件性措辞不参与 current Gate。 |
| `P04-004` | 论文须区分 current v1 结果与历史 v3 warning | 论文表述 | 中 | 已设边界 | 否 | 当前结果段只引用 current v1 自动生成资产；若叙述历史问题，必须注明 historical/non-current。 |
| `P04-005` | 后续模块比较可能受 seed 波动影响 | 下游比较风险 | 中 | 持续监控 | 否 | 后续阶段继续报告逐 seed、mean 和 std，并遵循自身 contract 与 Gate。 |
| `P04-006` | v1、v2、v3 与 future round 可能混用 | lineage / 污染风险 | 高 | 已隔离 | 否 | current manifest/summary 仅消费 six v1 contract runs；历史和 future assets 均不得混入。 |
| `P04-007` | Gate 命令返回码与报告正文曾不一致 | 门禁证据一致性 | 中 | 已关闭 | 否 | 仅以本次脚本实际返回码和重生成报告为准。 |
| `P04-008` | 旧 05 入口规则曾把旧 `Gate_B1` 当作默认阻断 | 流程契约一致性 | 高 | 已由 current v1 重裁 | 否 | 下游消费 current v1 的 `stage_pass_b1=true` 和 `handoff_ready_for_c1=true`；旧 false 值仅可在历史档案中引用。 |
| `P04-009` | v1 六 run 的派生表曾误写为 v3 identity | lineage / 派生表污染 | 高 | 已归档、已从 sample rows 重建 | 否 | 污染派生表/summary 仅保留在对应历史归档；current 表和 summary 必须由 `summarize_stage.py --stage b1 --stage-contract .../stage_contract.yaml` 自动生成。 |
| `P04-010` | frozen future reproduction 尚无正式运行证据 | future reproduction readiness | 中 | pre-run 完成；运行未开始 | 否（仅不允许 future round 放行） | 不得用 current v1 或历史指标、checkpoint、run_meta、metrics 证明 future round 通过。 |

## 4. historical v3 与 current v1 的不可混用边界

### 4.1 historical v3 事实

旧 v3 identity contamination 曾影响派生表与 summary 的身份表述，并伴随 stability warning、旧 `original_gate_b1=false` / `stage_pass_b1=false` / `handoff_ready_for_c1=false` 等历史结论。相关派生对象已进入对应 `_historical_archive/`，其身份为 historical/non-current，`valid_for_current_gate=false`。

这些事实用于说明为何必须重建 current 表，不是 current v1 的指标结论、Gate 输入或 handoff 判断。

### 4.2 current v1 事实

current summary 必须由以下自动化链条产生：

```text
six v1 original_protocol_reproduction contract runs 的 sample rows
→ scripts/summarize_stage.py --stage b1 --stage-contract b_class_auxiliary/coding_guards/04_Baseline/stage_contract.yaml
→ current baseline summary / per-seed summary / mean-std / comparison / manifest
```

该 summary 的 current flags 为：`complete_runs=true`、`main_metrics_not_worse=true`、`stability_not_weaker=true`、`gate_b1=true`、`stage_pass_b1=true`、`handoff_ready_for_c1=true`。本登记不手工生成或修改这些 flags。

### 4.3 禁止性规则

- 不得把 v3 warning、v3 identity、旧 `original_gate_b1=false` 或旧 handoff blocker 写成 current v1 conclusion。
- 不得手改 CSV、checkpoint、`run_meta.yaml`、metrics 或 summary 数值来改变 Gate。
- 不得用历史派生表替换、补充或校正 current v1 的脚本输入。
- 不得把 current v1 pass 移植为 future frozen reproduction 的运行结果。

## 5. current v1 handoff 与后续消费

current 04_Baseline 的编号阶段结论由自动生成 summary 裁定为 pass，C1 handoff 已准备就绪。后续阶段若消费 baseline，必须逐项记录真实 source identity：

```text
source_stage = 04_Baseline
source_manifest = reports/tables/baseline_stage_manifest.csv
source_protocol_version = train_proto_v1/eval_proto_v1
source_run_name = A2_UNet_GlaS_seed3407,A2_UNet_GlaS_seed1234,A2_UNet_GlaS_seed2025,B1_ResNet34_UNet_GlaS_seed3407,B1_ResNet34_UNet_GlaS_seed1234,B1_ResNet34_UNet_GlaS_seed2025
consumer_stage = <实际消费阶段>
consumer_file = <实际契约或 manifest>
consumption_boundary = current v1 original_protocol_reproduction only; historical v3 and future pending rounds excluded
```

后续阶段仍应报告逐 seed、mean、std，并按自身契约判定新模块收益；这是一项统计透明度要求，不是将历史 v3 warning 重新带入 current v1 Gate。

## 6. frozen future reproduction：独立且未运行

future round 的身份为 `frozen_reproduction_pending`，与 current v1 recovery 完全独立。以下 pre-run gates 已有独立 pass 报告：

- `frozen_reproduction_research_alignment_gate_report.md`；
- `frozen_reproduction_stage_definition_gate_report.md`；
- `frozen_reproduction_precheck_doc_gate_report.md`；
- `frozen_reproduction_stage_contract_check_report.md`。

这只证明研究定标、阶段锁定、Pre-check 和 stage contract 的准备性通过。以下项目**尚未运行**：isolated runtime、smoke、正式训练、正式测试、正式指标汇总、独立指标复核、code quality，以及 future round 专用正式 workflow gate。因此 future round 状态为 `pending_not_run`，不得声称训练、测试、runtime、smoke、复核、workflow 或正式 Gate 通过。

future round 的新 run_name/output_dir 必须保持独立；current v1 recovery 和任何历史资产只能作为审计边界，不得作为 future round 的结果证据。

## 7. 论文与报告表述边界

- 当前 04 结果只能引用 current v1 自动生成的 summary 和原始 six-run 资产。
- 若需说明 v3 污染整改，应使用“historical/non-current identity contamination 已归档；current v1 summary 从 contract sample rows 重建”的表述，不能把历史 warning 当作 current v1 局限。
- current v1 三 seed 设计不等于统计显著性检验，也不保证任意未来 seed 的结果。
- future frozen reproduction 在产生自身真实运行证据前，不能写入结果段、表格或通过结论。

## 8. 审计与维护

| 已读对象 | 当前用途 | 不可替代的机器结论 |
|---|---|---|
| `stage_contract.yaml` | 锁定 current v1 six-run 输入 | stage contract checker 的真实输出。 |
| `baseline_stage_summary.md` | current B1 Gate 与 handoff | `complete_runs`、主指标、稳定性、B1 Gate、stage pass 与 handoff flags。 |
| `workflow_gate_report.md` | current 04 流程总放行 | `workflow_gate_status`。 |
| future 专用四个 pre-run 报告 | future 准备性范围 | 仅代表 pre-run pass，不代表运行结果。 |
| `_historical_archive/` | v3 污染和旧结论溯源 | historical/non-current；不得作为 current Gate 输入。 |

## 9. 维护记录

| 日期 | 修改内容 | 依据 | 验证 |
|---|---|---|---|
| 2026-07-17 | 建立问题、统计边界和流程登记入口 | 当时阶段协议与真实产物 | 未修改实验结果文件。 |
| 2026-07-24 | 归档 v3 identity contamination 派生对象并建立 future round 的独立准备性对象 | six v1 run 原始资产、历史归档、future contract/config | current 派生表仅允许脚本从 v1 sample rows 重建；future 未训练。 |
| 2026-07-24 | 将旧 v3 warning / false Gate 从 current v1 口径中移出，明确 current v1 与 future round 的消费边界 | current `baseline_stage_summary.md`、stage contract、future pre-run reports | summary 重建为 `stage_pass_b1=true`；stage contract checker=`pass`（6 个既有输出目录 warning）；workflow gate=`blocked`，因为 runtime launcher 未设置 `PYTHONHASHSEED=3407`。 |
