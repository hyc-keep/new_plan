# 01-04 全链阶段边界、执行顺序与审计记录

- 状态：`pre-run only / pending_not_run`。
- 审计结论：历史 01 数据物理资产、split 和当前 hash 闭合；历史 02 A1 checkpoint identity/hash lineage 未闭合；历史 03 A2 同名输出被 04 重用且 aggregate 与当前 sample CSV 不一致。

## 唯一执行顺序

1. 运行链检查器并生成/复核 `01_data_freeze_manifest.json`，确认四项 hash 等于主 contract。
2. 仅运行新 `A1_UNet_GlaS_seed3407_chain`，生成 02 新 manifest 后才能进入 03。
3. 仅运行三个新 `A2_UNet_GlaS_*_chain`，生成 03 新 A2 manifest 后才能进入 04。
4. 04 读取一次 03 新 A2 manifest，仅运行三个新 `B1_ResNet34_UNet_GlaS_*_chain`。不得重新运行 A2。

## 禁止边界

- 不得修改 `experiments/**` 下任何历史结果、CSV、checkpoint 或 `run_meta`。
- 不得消费历史 A1、A2、B1 目录、历史 aggregate、historical archive 或现有 04 stage contract 的输出作为新链输入。
- 不得因本轮文档或预运行检查而填入任何未来指标、通过结论或 checkpoint 哈希。

## 审计状态

本轮仅完成结构、配置和静态预运行检查。未来 02/03/04 manifest 均保留为 `*_pending.yaml` 路径契约，当前故意不存在；它们只能由对应阶段真实运行完成后的脚本生成。
