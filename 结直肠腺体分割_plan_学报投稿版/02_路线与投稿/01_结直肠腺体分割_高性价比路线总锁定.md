# 结直肠腺体分割高性价比路线总锁定

## 文档身份

- 归属：A 类，当前/未来正式路线总协议；不是实验结果报告。
- 当前路线：`B1（historical_provenance_only） -> N1 -> N2 -> N3`。
- 编号阶段：`05_方法协议 -> 06_val筛查 -> 07_正式GlaS -> 08_CRAG验证 -> 09_外部对比 -> 10_结果汇总 -> 11_总验收与止损`。
- 状态：B1=`valid_with_stability_warning`；N1/N2/N3=`planned/not_run`。本文件不得预写 research/stage pass。
- 结果状态：`formal_result=not_run`；`result_eligibility=false`；任何真实运行字段当前为 `not_created`。
- 治理边界：formal checker 必须以本轮实际变更文档为输入重跑；旧空输入 `not_applicable` 回执不构成 pass。即使本轮 formal checker 通过，也不等于 research gate、stage lock、implementation permission 或实验完成。

## 本轮重写直接依赖的前置文件

- 上游：`01_实验执行/00_总览与规范/00-07`，提供 active root、阶段、参数、评估、字段、代码接口和质检真源。
- 同层：`02_路线与投稿/02-06`，分别提供分阶段执行、投稿止损、可靠性复核、研究定标和最低证据包。
- 证据：`03_文献证据/05_腺体任务经典论文/01_GlaS-Challenge.md`、`03_DCAN.md`、`04_MILD-Net.md`、`07_TA-Net.md`、`12_DEA-Net.md`，以及 `02_边界_形状_损失支撑/01_Gated-SCNN.md`。
- 下游：`01_实验执行/05_方法协议/00-04` 与 `06-11`，消费方法身份、盲态、lineage、验收和回退。

## 上游、同层与下游

- 上游：`01_实验执行/00_总览与规范/00-07` 与 `03_文献证据`，提供全局状态、评估身份、数据边界和科学依据；本文件不能覆盖这些冻结规则。
- 同层：本目录 02-06，分别承接阶段顺序、投稿裁决、可靠性、研究定标和最低证据包；同层文件必须保持路线、状态和 lineage 一致。
- 下游：`01_实验执行/05_方法协议` 至 `11_总验收与止损`，消费本文件的路线身份和候选边界；下游结果不得反向改写本路线。

## 路线依据

1. 旧工程与旧 `LKMA -> Boundary -> Distance` 只提供历史来源，不是当前 Gate、当前工程落点或当前待运行路线。
2. B1 只读基座先保证同数据、同训练、同评估可追溯；其 warning 必须进入局限，不得改写成“稳定最佳”。
3. N1 只增加实例感知轮廓监督；N2 只增加轻量区域/轮廓融合；N3 只增加轮廓引导的 marker-controlled watershed。一次只开一个机制变量，失败可独立定位和回退。
4. 06 只用 train68/val17 筛查；TestA/TestB 与 CRAG test 在候选冻结前保持 blind，不得反向选择模型、阈值、marker 或后处理。
5. Plan B 只能由 N3 validation 机制门触发一次，身份固定为 `DCAN-inspired object-contour joint inference adaptation`，必须提交差异报告，不得称 strict replication。

## 文献依据

- GlaS Challenge 论文给出 TestA/TestB 分开报告和对象级指标语义；Object F1 的正式 primary identity 采用 `overlap/|G_i| >= 0.5`。
- 本地 DCAN 风格代码使用 `>0.5`，只作为 compatibility audit；必须做恰好 `0.5` 的边界单测与差异报告，不声称官方脚本已核验。
- DCAN 支持对象/轮廓联合建模方向，但原文未给齐本工程 strict replication 所需的全部结构与训练细节。
- MILD-Net、TA-Net、DEA-Net 和 Gated-SCNN 支持边界、注意或多尺度作为问题动机与相邻方案；不构成把其默认值直接冻结为本项目参数的依据。

## 固定、可调与禁止参数

- 固定：active route、01-11 阶段顺序、B1/N1/N2/N3 方法身份、GlaS/CRAG split 角色、TestA/TestB/CRAG test 盲态、Object F1/Boundary F1 identity 和七字段 lineage。
- 可调：仅限 05/06 阶段预注册的机制候选、阈值和后处理参数；选择只能使用 val，并记录版本和理由。
- 禁止：旧路线作为 current、测试集反馈、无限候选扩展、planned 接口冒充实现、历史结果进入 current Gate。

## 方法与公式边界

- 区域 Dice：`Dice=2|P∩G|/(|P|+|G|)`。
- Object F1：`F1_obj=2TP/(2TP+FP+FN)`；primary TP 使用挑战论文语义 `overlap/|G_i| >= 0.5`。
- N1：逐实例生成 contour target；具体宽度、loss 权重由 05 协议冻结，06 只在预注册有限候选内筛查。
- N2：只允许一个轻量融合点；不得并行叠加新 backbone、attention 或 distance loss。
- N3：marker/elevation、阈值与最小面积只由 val17 决定；test 不回调。

## A/B/C 与差异依赖

| 对象 | 归属 | 当前角色 | 差异依赖 |
|---|---|---|---|
| 本文件、路线 02-06、05 方法协议、06-11 阶段协议 | A | 当前/未来正式协议、验收与投稿边界 | 必须与 active root、阶段号、状态和 lineage 同步 |
| 检查清单、审计对表、模板、manifest | B | 流程与证据辅助 | 不得代替 A 类正文或 Gate 事实 |
| 同时声称当前路线又保留旧路线执行指令的混合件 | C | 待裁决 | 先剥离历史 provenance，再允许消费 |

任何 C 类对象不得同时承担正式协议和辅助审计职责。旧路线内容只允许在历史归档或本文件冲突裁决中出现一次，不得成为当前操作指令。

## 七字段 planning 实例

```yaml
source_stage: s05_method_protocol
source_manifest: planned:reports/stage_reports/s05_method_protocol_manifest.json
source_protocol_version: method_protocol_v1
source_run_name: not_applicable_protocol_only
consumer_stage: s06_val_screening
consumer_file: 01_实验执行/06_val筛查/00_阶段总协议.md
consumption_boundary: protocol_and_lineage_only; train68_val17_only; no_TestA_TestB_CRAG_test
```

`planned:` 表示接口尚未由 accepted artifact 创建，不得冒充真实 manifest。七字段缺一、路径不在 active root、hash 不一致或 blind_state 不合规时，下游 Gate 必须 blocked。

## 代码落地接口

- active project root：`/home/featurize/work/Paper/crc_gland_segmentation_project_journal`。
- 计划接口：`src/data/targets/instance_contour.py`、`src/models/heads/contour_head.py`、`src/models/modules/region_contour_fusion.py`、`src/postprocess/contour_guided_watershed.py`。
- 评估接口：`src/metrics/object_metrics.py`、`src/eval/run_eval.py`；若尚未创建，状态必须写 `planned_not_created`。
- 同步对象：`plan/config/run_name/run_meta/manifest/gate/handoff`。
- 运行字段：`stage/status/planned_or_actual/dataset/split/blind_state/seed/config_digest/code_digest/checkpoint/metric_identity/decision/rollback_target` 加七字段 lineage。

## 验收与 blind / handoff

- 05 只冻结方法身份、参数边界、接口和回退，不写效果。
- 06 只消费 train68/val17；N1 不通过不得进 N2，N2 不通过不得用 N3 掩盖。
- 07/08 只消费 06 accepted 且锁定的候选；TestA/TestB 分开报告，CRAG test 只作盲态补充验证。
- 09 外部对比共享相同 split、训练预算、评估 identity 和结果标记。
- handoff 必须含 accepted manifest、七字段、blind_state、唯一变量、metric identity 与 rollback target；截图和口头结论不可消费。

## 独立回退条件

- active root、阶段号、状态、七字段、blind_state 或 metric identity 冲突：回治理/研究定标，不编码。
- Object F1 边界单测或 primary/compatibility 差异报告缺失：对象级结果不得进入正式主表。
- N1 失败回 B1；N2 失败回 N1；N3 validation 机制失败且排除实现错误后才允许一次 Plan B。
- Plan B 失败回 N2 或 B1，停止扩模块；不得恢复旧 LKMA/Boundary/Distance 当前路线。
- 发生 test 驱动选择或盲态泄漏：相关结果作废，回到最近未泄漏的 accepted manifest。

## 冲突裁决记录

1. 旧根、旧工程和旧编号与 active journal 路线冲突：前者仅 `historical_provenance_only`，不得生成 current Gate。
2. 旧 LKMA/Boundary/Distance 当前态与 N1-N3 冲突：当前唯一方法路线为 N1-N3；旧结果不删除、不重跑、不改名。
3. “strict DCAN”与证据不足冲突：统一改为 adaptation，并要求差异报告。
4. Object F1 `>=0.5` 与本地 DCAN `>0.5` 冲突：前者为 primary，后者仅 compatibility audit。
5. formal governance pass 与 research/stage 状态冲突：只陈述已存在 formal pass，不预写 research/stage pass。

## 审计对表

| 已读文件 | 正文落点 | 自检落点 | Diagnostics | 缺口/修复状态 | 对照模板 | 补齐项 |
|---|---|---|---|---|---|---|
| 规范 00-07 | 身份、字段、接口、回退 | 下表 1-4 | 待本轮实跑 | 已统一 active root/阶段/状态 | 路线 02 | 补 A/B/C、七字段、blind |
| 路线 02-06 | 路线、投稿、止损 | 下表 2-5 | 待本轮实跑 | 已清除旧路线当前态 | 路线 02 | 补独立 handoff |
| GlaS/DCAN 证据 | 文献与 Object F1 | 下表 3 | 待本轮实跑 | 已裁决双 identity | 规范 04 | 补边界单测要求 |
| 05 当前协议 | 下游接口与回退 | 下表 4-6 | 待本轮实跑 | 不预写 stage pass | 05/00 | 补 planning 实例 |

## 文件质量自检

- [x] A 类身份、上游/同层/下游差异依赖明确。
- [x] 当前路线、文献、公式、参数边界、代码接口和字段可定位。
- [x] Object F1 primary/compatibility 身份、边界单测和差异报告明确。
- [x] 七字段 planning 实例、blind 与 handoff 可执行。
- [x] 验收和独立回退分章，未预写研究、阶段或实验 pass。
- [x] 旧根、旧路线、旧文献计数和 strict DCAN 当前态已清除。
- [x] 审计对表强度不低于同层路线 02；正文与自检声明一致。

## Diagnostics 闭环

本轮须运行章节、路径、禁词、lineage、Markdown diagnostics 和适用 formal checker。任何 unresolved path、旧路线当前态、strict replication、旧文献计数当前口径、状态越级或七字段缺失均先回写本文件；实际命令与结果以本轮最终回执为准。
