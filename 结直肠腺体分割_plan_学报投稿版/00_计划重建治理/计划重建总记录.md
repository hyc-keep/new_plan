# 学报投稿版计划重建总记录

- record_id: `journal_plan_rebuild_20260720`
- active_plan_root: `/home/featurize/work/Paper/结直肠腺体分割_plan_学报投稿版`
- historical_plan_root: `/home/featurize/work/Paper/结直肠腺体分割_plan_优化版`
- active_project_root: `/home/featurize/work/Paper/crc_gland_segmentation_project_journal`
- historical_project_root: `/home/featurize/work/Paper/crc_gland_segmentation_project`
- status: `rebuild_in_progress`
- experiment_permission: `blocked_until_plan_rebuild_complete`
- result_eligibility: `false`
- created_at: `2026-07-20`

## 1. 用户要求

按照旧计划的严谨程度，完整重建学报投稿版计划体系。每个正式 Markdown 文件必须能够直接支撑研究定标、阶段锁定、代码实现、实验执行、论文写作、阶段验收和回退；所有参数、模型、指标、split、变量、候选范围和投稿最低证据必须有来源、冻结规则和执行落点。计划重建完成前禁止正式实验执行。

## 2. 来源边界

### 当前有效来源

- 当前学报投稿版计划正文：当前轮需要重写的 active 文档。
- 当前 journal 工程：只用于确认实际代码、配置、数据和工程落点。
- 正式参考资料：论文、官方页面快照、任务 benchmark、指标实现、参考代码和许可证记录。

### 历史参考来源

- 旧优化版计划：作为流程、协议、参数和投稿结构的规范基线。
- 旧工程：作为训练、验证、测试、checkpoint、汇总和 handoff 的接口基线。

旧计划和旧工程的历史结果、checkpoint、CSV、summary、Gate 和 handoff 不得直接进入当前计划结果链。

## 3. 当前已完成的审计

| 审计对象 | 已读范围 | 已确认结论 | 当前状态 |
|---|---|---|---|
| 旧计划 | `旧计划/01_实验执行`、`02_路线与投稿`、`03_文献证据` | 旧版具有完整阶段协议、参数冻结、评估、验收、投稿 readiness 和止损结构 | 已审计 |
| 新计划 | `新计划/01_实验执行`、`02_路线与投稿`、`03_文献证据` | 文献层基本保留，但 01、02 和执行接口仍存在计划态、缩水和实现断层 | 已审计 |
| 旧工程 | train/eval/test/summarize/config/tests | 旧工程具备完整 train → val → checkpoint → testA/testB → raw → aggregate → handoff 主链 | 已审计 |
| 正式参考资料 | GlaS、DCAN、MILD-Net、任务 benchmark、surface-distance、MedPy、基线/边界/大核代码 | 核心论文、任务指标和参考代码足够；需记录版本、许可证和适用边界 | 已审计 |
| skill 体系 | 根 skills、旧工程 skills、journal 导航和计划重写 skill | 当前需唯一入口、计划专用 skill、逐文件重写和 diagnostics 闭环 | 已审计 |

## 4. 已发现的必须返工问题

1. 新 N1 训练入口曾把 smoke batch 上限泄漏到正式训练，导致正式每 epoch 只执行极少 batch；相关 run 已判无效并归档。
2. 新 N1 入口只记录 loss，没有接入旧工程级别的正式 Object F1/Object Dice/Object Hausdorff、Dice、IoU、HD95、Boundary F1 测试链。
3. 新 N1 使用 `val_loss_min` 的 checkpoint 选择，与旧正式协议 `val_objdice_max` 存在身份冲突，必须在计划层裁决后才能编码。
4. 新计划声明了 blind、lineage、handoff 和 manifest，但若代码接口未真实实现，不能视为实验闭环。
5. 新计划 11 阶段部分细粒度投稿 readiness 和旧版验收文件被降级/归档，必须重新判断是否需要恢复为当前有效文件。
6. 当前计划存在“治理文档看起来完整，但正式生产、评估、汇总、handoff 未闭合”的问题。

## 5. 固定重建顺序

| 批次 | 重建范围 | 主要输出 | 状态 |
|---|---|---|---|
| 0 | 治理、清单、来源矩阵、差异矩阵、专用 skill | 本记录、文件清单、来源矩阵、差异矩阵、`crc-gland-plan-rebuild` | 进行中 |
| 1 | `01_实验执行/00_总览与规范` | 8 个全局规范文件及诊断 | 已完成文档重写，formal_doc_gate=pass；实验仍 blocked |
| 2 | `02_路线与投稿`、`03_文献证据` 映射 | 路线、投稿、证据映射和写作准入 | 批次 2 路线文件已修补；03 文献证据逐文件审计未完成 |
| 3 | `01_数据协议` | 数据身份、split、标签、资产、验收、handoff | 未开始 |
| 4 | `02_UNet流程验证`、`03_UNet稳定性`、`04_Baseline` | 基线流程、三 seed、checkpoint、测试和阶段 Gate | 未开始 |
| 5 | `05_方法协议`、`06_val筛查`、`07_正式GlaS` | 新方法、候选筛查、正式三 seed 和正式结果契约 | 未开始 |
| 6 | `08_CRAG验证`、`09_外部对比` | 外部验证、公平对比和引用值/复现值边界 | 未开始 |
| 7 | `10_结果汇总`、`11_总验收与止损` | raw、aggregate、failure、投稿结果包、Plan B 和总 Gate | 未开始 |
| 8 | 全部 active plan diagnostics、skill 导航和治理收口 | 计划完成定义和实验冻结裁决 | 未开始 |

## 6. 每个正式文件必须登记的字段

- 文件绝对路径和 A/B/C 归属。
- 旧计划对应文件和章节。
- 新计划本文件承担的唯一职责。
- 上游、同层、下游文件。
- 论文/文献证据文件与章节、公式或结论。
- 官方页面、官方脚本或参考代码路径。
- 固定参数、可调参数、禁止修改参数。
- 代码落地接口和输入输出资产。
- 运行记录字段和 manifest 字段。
- 验收条件、回退条件和止损条件。
- 当前状态：`not_started / in_progress / verified / blocked`。
- diagnostics 命令和真实结果。

## 7. 已创建对象

| 对象 | 路径 | 状态 |
|---|---|---|
| 新计划专用 skill | `/home/featurize/work/Paper/crc_gland_segmentation_project_journal/.trae/skills/crc-gland-plan-rebuild/SKILL.md` | 已创建，待导航接入和验证 |
| 计划重建总记录 | `/home/featurize/work/Paper/结直肠腺体分割_plan_学报投稿版/00_计划重建治理/计划重建总记录.md` | 已创建，持续更新 |
| 文件清单 | `00_计划重建治理/计划文件清单.md` | 待创建 |
| 来源矩阵 | `00_计划重建治理/计划来源与实现矩阵.md` | 待创建 |
| 差异矩阵 | `00_计划重建治理/旧新版差异矩阵.md` | 待创建 |
| 批次审计表 | `00_计划重建治理/批次审计对表.md` | 待创建 |

## 8. 批次 1 实际修改与验证记录

- 修改文件：`01_实验执行/00_总览与规范/00_执行导航.md`、`01_工程目录框架.md`、`02_参数冻结总表.md`、`03_命名与结果记录规范.md`、`04_评估口径与官方脚本对齐.md`、`05_代码工程映射与实现策略.md`、`06_实验执行证据化写作模板.md`、`07_实验执行全局修订与质检规范.md`。
- 重写依据：旧计划 00_总览与规范 8 文件；正式参考资料 GlaS/DCAN/MILD-Net/GlandSegBenchmarks/surface-distance/MedPy；旧工程 train/test/trainer/run_eval/checkpoint_selector/summarize_stage；`standard-md-rewrite`。
- 真实修复过程：初次 checker 为 `partial`，暴露低正文行数和 planned interface 锚点问题；经过三轮实质补写和路径状态修正后，`formal_doc_gate_batch_1_v4.md` 为 `formal_doc_gate_status=pass`。
- 运行命令：`check_formal_stage_docs.py --current-round plan_rebuild_batch_1_global_specs_v4 --docs <8 active global docs> --output formal_doc_gate_batch_1_v4.md`。
- 其他检查：`check_precheck_docs.py` 命令已执行；当前结果写入 `precheck_batch_1_v2.md`，不把其结果扩写为实验通过。
- 未运行：训练、测试、预测、指标、汇总、checkpoint 选择、workflow Gate。
- 批次结论：全局文档结构通过，参数和正式实验仍未冻结完成；下一批次必须继续重写路线/投稿和文献映射。

## 8. 当前阻塞项

- 尚未完成全部逐文件来源矩阵。
- 尚未完成旧版与新版 01、02、03 的逐文件差异矩阵。
- 尚未重写任何 active 正式阶段正文。
- 尚未为每个 active Markdown 运行 diagnostics。
- 尚未恢复旧版完整正式训练/评估/汇总/handoff 接口到 journal。
- 尚未裁决 N1/N2/N3 的最终方法身份、参数和 checkpoint selector。
- 尚未建立计划完成后的实验冻结 Gate。

## 9. 实验执行硬阻断

在以下条件全部满足前，禁止启动正式训练、正式测试、正式预测、结果汇总或投稿 readiness 判断：

1. 文件清单、来源矩阵、差异矩阵和批次审计表完成。
2. 全部 active 计划正文按批次重写并通过对应 diagnostics。
3. 旧计划流程与新方法变量的边界已锁定。
4. 参数、模型、loss、target、metric、checkpoint selector、threshold、postprocess、split、seed 和统计口径已冻结。
5. 旧工程训练/评估/测试/汇总/handoff 接口已完成当前 active root 适配或被正式裁决不采用。
6. 研究定标、阶段锁定、Pre-check 和代码质量 Gate 真实通过。
7. `workflow_gate_report.md` 尚未通过时，不得写学习型说明文或投稿结论。

## 10. 本批 00-07 重写回执
- 修改文件：active `01_实验执行/00_总览与规范/00_执行导航.md` 至 `07_实验执行全局修订与质检规范.md`，共 8 个；未修改旧计划、旧工程或历史归档。
- 依据：旧计划同名 00-07；active `02_路线与投稿/01-06`、`03_文献证据`；GlaS/DCAN/MILD-Net；GlandSegBenchmarks/DCAN `metrics.py`、`test_GlaS.py`、surface-distance、MedPy；旧工程接口仅作 historical provenance。
- 适配：统一 active plan root 与 `/home/featurize/work/Paper/crc_gland_segmentation_project_journal`；路线为 01-11，N1/N2/N3 仅属 05；所有 formal result 为 `not_run`，资格为 `false`。
- 诊断命令：`python /home/featurize/work/Paper/crc_gland_segmentation_project_journal/b_class_auxiliary/tools/check_formal_stage_docs.py ... --current-round journal_plan_rebuild_20260720`；`python /home/featurize/work/Paper/crc_gland_segmentation_project_journal/b_class_auxiliary/tools/check_precheck_docs.py --project-root /home/featurize/work/Paper/crc_gland_segmentation_project_journal --precheck-guard b_class_auxiliary/coding_guards/05_method_protocol/N1/Pre-check Guard.md`。
- 结果：formal checker 多次真实运行，最新状态仍为 `partial`，报告 `/home/featurize/work/Paper/crc_gland_segmentation_project_journal/formal_doc_gate_report.md`；Pre-check 最新状态 `pass`，报告 `/home/featurize/work/Paper/crc_gland_segmentation_project_journal/b_class_auxiliary/coding_guards/05_method_protocol/N1/precheck_doc_gate_report.md`。
- 未完成项：formal checker 仍有正文有效行数/残余路径锚点提示；未启动任何训练、测试、预测或汇总。

## 11. 批次 2 路线/投稿修补记录

- 批次标识：`plan_rebuild_batch_2_route_submission_repair_20260720`。
- 修改文件：`02_路线与投稿/01-06` 六个 active Markdown；未修改旧计划、旧工程、代码、训练资产、结果资产和历史归档。
- 正文修补：补齐差异化上游/同层/下游、路线与文献依据、固定/可调/禁止参数、运行记录字段、代码落点、冲突裁决、独立回退、文件质量自检和审计闭环；`planned_not_created` 不得被解释为已实现。
- 统一裁决：当前路线为 `01 -> 11`；N1/N2/N3 仅为 05 方法身份；B1 为 `historical_provenance_only/valid_with_stability_warning`；Plan B 仅为 `DCAN-inspired object-contour joint inference adaptation`，只能由 N3 val mechanism gate 触发一次；新结果保持 `formal_result=not_run`、`result_eligibility=false`。
- 依据：active `01_实验执行/00_总览与规范/00-07`；旧计划对应路线文件；`03_文献证据` 中 GlaS/DCAN/MILD-Net/边界与实例恢复材料；参考实现 DCAN metrics/test_GlaS、surface-distance、MedPy；旧工程仅作为 historical interface provenance。
- 参数与变量边界：固定 GlaS train68/val17 screening、TestA/TestB/CRAG test blind、正式 seed `3407/1234/2025`、Object F1 primary `>=0.5`、DCAN `>0.5` compatibility、唯一 Boundary F1 identity；可调项只能来自 val 预注册 candidate budget；禁止测试反馈、阶段跳转和历史结果回流。
- 代码状态：路线文档只登记 active journal planned/current 接口；未创建 N2/N3 模块与未正式接入的统一 train/eval/test/summarize 接口均保持 `planned_not_created` 或 `blocked`，没有因此创建实验许可。
- 审计状态：本轮正式 checker 尚未运行；旧 `formal_doc_gate_report.md` 的 `not_applicable` 不作为本轮结论。待显式传入 6 个目标文件运行，并如实记录 `pass/partial/fail`。
- 当前缺口：`03_文献证据` 逐文件正文审计未完成；全 active plan diagnostics 未完成；`02_路线与投稿` 的正式 checker 专用适配尚不存在；研究定标、stage lock、Pre-check、代码质量和 workflow Gate 未通过。
- 是否允许进入下一批次：不允许进入实验；在路线批次 checker 真实结论前，不进入后续阶段正文重写。

## 12. 批次 3 文献证据核心修补记录

- 批次标识：`plan_rebuild_batch_3_literature_core_repair_20260720`。
- 修改文件：`03_文献证据` 顶层 7 个 active Markdown；未修改历史归档、旧计划、旧工程、代码和实验结果。
- 实际修补：统一 `62/62` 和 8 个正式目录计数；修正 `01` 中旧 `LKMA -> Boundary -> Distance` 与 current route 冲突；将 `B1 -> N1 -> N2 -> N3` 写为当前实验主线；为模板、索引、路由、速查表和结果模板补齐证据状态、代码边界、固定/可调/禁止、运行字段、七字段 lineage、验收、独立回退、冲突裁决、质量自检、Diagnostics 和审计对表。
- 文献结果边界：`05_GlaS_CRAG_主结果数值速查表.md` 的数字统一视为 `quoted_from_original_paper/reference_only`；`06` 只允许 accepted run bundle 进入 current 表；本项目结果保持 `formal_result=not_run`、`result_eligibility=false`。
- 代码状态：文献文件只登记 active journal planned manifest、identity validator、routing/keyword lookup 和结果模板接口；不存在接口保持 `planned_not_created`，未调用训练/测试/汇总。
- 诊断结果：顶层 7 文件专项结构与 lineage diagnostics=`pass`；7/7 文件均具备 8 个显式收尾标题和七字段 lineage。历史 `check_formal_stage_docs.py` 不适用于 `03_文献证据`，未将其退出码误记为 pass。
- 当前缺口：下层 62 篇单篇深提取稿仍需逐篇核验 PDF 路径、页码、公式、数字、指标 identity、代码状态和 `quoted/reproduced` 标记；单篇缺口不得由索引通过替代。
- 是否允许进入实验：不允许。文献核心修补通过不等于研究定标、stage lock 或实验 Gate 通过。

## 13. 批次 4 单篇文献证据状态修补记录

- 批次标识：`plan_rebuild_batch_4_single_paper_evidence_status_20260720`。
- 扫描范围：`03_文献证据/01-08` 八个正式目录，共 62 个单篇 Markdown；未扫描历史归档作为 current evidence，也未修改历史归档。
- 实际修补：62/62 单篇稿末尾补齐 `证据状态与当前消费边界`、`代码落地接口`、`运行记录字段与 lineage`、`独立回退条件`、`冲突裁决记录`、`文件质量自检`、`Diagnostics 闭环`、`审计对表`。
- 统一状态：每篇固定 `evidence_status=unverified`、`formal_result=not_run`、`result_eligibility=false`、`quoted_or_reproduced=quoted_from_original_paper/reference_only`；七字段 lineage 已统一，`source_stage=s03_literature_evidence`、`source_run_name=reference_only`、`consumption_boundary=literature_evidence_only_no_current_result`。
- 未解决事实：62/62 仍需人工核验 PDF 路径、页码、公式、数字、split、指标 identity、代码 repository/commit/tag/key files；原有 `待确认`、`待填` 和空白字段保持不变，未虚构补值。
- Diagnostics：单篇状态结构检查=`pass`，62/62 字段覆盖；单篇证据 Diagnostics=`partial_pending_manual_source_review`，62/62，不能写成 pass。
- 实验权限：继续 `blocked`。单篇结构化修补不等于文献证据已核验，不授予研究定标、stage lock 或训练权限。

## 14. 批次 5 来源资产核验记录

- 批次标识：`plan_rebuild_batch_5_source_asset_audit_20260720`。
- 核验范围：`03_文献证据/01-08` 八个正式目录的 62 篇单篇稿；未修改顶层索引、历史归档、旧计划、旧工程、代码和实验结果。
- 真实核验结果：62/62 单篇稿存在 `PDF 路径` 字段；对应本地 PDF 文件存在数为 62/62；不存在 PDF 路径缺失或文件缺失。
- 内容状态：62/62 保持 `paper_identity_status=not_independently_rechecked`、`page_formula_metric_split_status=manual_review_pending`、`code_repository_status=manual_review_pending`；`verified_pdf_page=0/62`，不能宣称论文内容已逐篇核验。
- 实际修补：每篇新增 `来源资产核验` 区段，明确 PDF 存在与正文证据核验的区别、回退条件和审计对表；未删除 `待确认`/`待填`，未虚构数字、页码、公式或代码 commit。
- Diagnostics：`source_audit_status=pass_structural_partial_content`；结构和资产存在性通过，内容复核保持 `partial_pending_manual_source_review`。
- 实验权限：继续 `blocked`。来源文件存在不等于文献证据通过，不授予研究定标、stage lock 或训练权限。

## 15. 批次 6 数据协议阶段修补记录

- 批次标识：`plan_rebuild_batch_6_data_protocol_repair_20260720`。
- 修改范围：`01_实验执行/01_数据协议/00-07` 共 8 个 active Markdown；未修改旧计划、旧工程、历史归档、代码、数据和结果。
- 主要修补：统一 active plan/project root、01-11 route、N1/N2/N3 方法归属、旧 LKMA/Boundary/Distance historical provenance、GlaS/CRAG split、blind 边界、输入输出资产、CSV/schema、manifest/hash、代码接口状态、七字段 lineage、验收、回退、冲突、自检和审计对表。
- 当前固定口径：GlaS `train68/val17/TestA60/TestB20`，`split_seed=3407`；CRAG `train153/val20/test40`，`train153=train_sup16 union train_unsup137`，`split_provenance_type=project_frozen_from_local_layout`，`official_universal_split_claim=false`；验证集只用于 selector/threshold/postprocess，TestA/TestB/CRAG test 保持盲态。
- 文档 diagnostics：显式运行 `check_formal_stage_docs.py --docs`，报告 `/tmp/active_data_protocol_formal_doc_gate_v3.md`，真实状态 `formal_doc_gate_status=pass`。
- 真实资产状态：source manifest、CSV、preview、标签产物、digest、preflight 和 data-stage Gate 尚未真实执行；数据阶段仍为 `planned/blocked`，`formal_result=not_run`，`result_eligibility=false`。
- 额外检查说明：一次 `git diff` 因当前工作区不是 Git 仓库而失败，不影响正式文档 checker；未将该命令失败误记为文档通过或实验通过。
- 是否允许进入实验：不允许。必须先完成真实数据资产生成/检查/manifest/hash/preflight 和 data-stage handoff，之后才能进入后续阶段。

## 16. 批次 7 active project 数据接口只读审计记录

- 批次标识：`plan_rebuild_batch_7_data_interface_readonly_audit_20260720`。
- 只读范围：active project 的 `configs/data/glas.yaml`、`configs/data/crag.yaml`、`src/data/datasets.py`、`splits/glas/README.md`、`splits/crag/README.md`、`source_copy_manifest.json`；未执行脚本、未修改配置、未生成数据。
- 已确认的 existing 接口：`configs/data/glas.yaml`、`configs/data/crag.yaml`、`src/data/datasets.py`、两个 split README、source copy manifest 均存在；`src/data/datasets.py` 使用 CSV loader 接口，但正式 split 生成脚本、配对检查脚本、数据资产校验脚本和 manifest 构建脚本未发现。
- 确认的阻塞：`glas.yaml` 的 `dataset_root` 当前指向 journal 工程根，不是明确的 GlaS 数据资产目录；`crag.yaml` 的 `dataset_root` 仍指向历史旧工程根；两个配置的 CSV 文件和 source note 资产尚未证明可消费；README 明确标记 split 尚未生成。
- 代码边界：这些问题属于 active project 配置/数据接口实现，不在本轮计划 Markdown 修补中直接修改；后续必须在数据协议 Gate、研究定标和 stage lock 允许后，由代码实现批次修复并进行独立 preflight。
- 结果资格：当前仍为 `formal_result=not_run`、`result_eligibility=false`；历史实验目录、N1 smoke/full split 资产和 source copy manifest 不得代替当前数据 Gate。
- 是否允许进入下一批次：允许继续计划文档审计，但不允许进入正式训练、测试、预测、结果汇总或投稿判断。

## 17. 后续更新规则

每完成一个批次，必须更新本文件：

- 修改了哪些文件。
- 每个文件依据什么。
- 固定了哪些参数和变量。
- 运行了哪些 diagnostics。
- diagnostics 的真实结果。
- 发现了哪些缺口。
- 哪些缺口已修复。
- 哪些工作仍未完成。
- 是否允许进入下一批次。

本记录不保存实验指标，不替代正式结果 CSV/JSON/YAML，不把规划状态写成实验结果。
