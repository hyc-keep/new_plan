# 参数冻结总表

## 1. 文件角色与执行边界
本文件是 active journal 全局参数和状态真源，归属 A 类。它冻结允许值、候选边界、变更条件和代码字段，不宣称实验通过。`status=planning_rewrite_verified_pending_diagnostics`，`formal_result=not_run`，`result_eligibility=false`。

## 2. 本轮直接依赖的前置文件
旧计划同名 02 是规范基线；active 00/01/03/04/05/06/07 提供路线、目录、命名、评估、接口和质检约束；`02_路线与投稿/01-06` 与 `02_路线与投稿/05_研究定标记录.md` 提供当前 N1-N3 边界；GlaS/DCAN/MILD-Net 提供 benchmark、对象和轮廓方法依据；/home/featurize/work/Paper/结直肠腺体分割_正式参考资料/04_task_specific_benchmarks/GlandSegBenchmarks-master/DCAN/metrics.py、/home/featurize/work/Paper/结直肠腺体分割_正式参考资料/04_task_specific_benchmarks/GlandSegBenchmarks-master/DCAN/test_GlaS.py、surface-distance、MedPy 只作为实现/交叉核对来源。

## 3. 本阶段唯一允许处理的变量
只处理全局参数的冻结值、候选边界、版本和变更条件；不在本文件写运行结果。

## 4. 阶段门控表达式
`parameter_gate = every_consumed_field_registered AND version_sync AND blind_preserved`；否则为 `blocked`。

## 上游、同层与下游
上游是路线和研究定标，决定哪些参数已锁定；同层 01/03/04/05 将参数落入目录、身份、评估和代码；下游 01-11 消费本表的 config/version/lineage，不得自行重开全局参数。参数表只记录协议，不写任何结果。

## 路线依据与文献依据
当前路线为 `01-04` 前置资产层、`05` 方法身份、`06` val 筛查、`07` 正式 GlaS、`08` CRAG、`09` 外部对比、`10` 汇总、`11` 总验收。GlaS 固定 benchmark 和 TestA/TestB 盲态；DCAN 支撑 object-contour 语义；MILD-Net 支撑同任务对象级评价。论文值、候选值和工程值必须分开标记。

## 冻结表
| 对象 | 当前结论 | 规则类型 | 固定/可调/禁止 | 代码与记录落点 |
|---|---|---|---|---|
| `active_route` | `journal` | 工程冻结规则 | 固定 | config、run_meta |
| stage order | `01..11`；N1/N2/N3 仅属 05 | 路线层已锁定 | 固定 | stage manifest |
| GlaS | train68/val17；TestA/TestB blind | 官方协议固定项 | 固定 | `configs/data/glas.yaml`、`splits/glas/` |
| CRAG | train/val/test 按 active manifest | 路线层已锁定 | 固定 | `configs/data/crag.yaml`、`splits/crag/` |
| 输入 | `512x512`，如 stage card 无新裁决则继承 | 论文支持的候选范围 | stage 前冻结 | `configs/data`、transforms |
| loss/optimizer | 继承 B1 冻结协议；N1-N3 仅按 stage card 单变量改变 | 工程冻结规则 | 可调但须裁决 | `configs/train`、`src/losses` |
| threshold | 初始 `0.5`；正式值只由 val 预注册候选锁定 | 工程冻结规则 | val 可调，test 禁调 | `src/eval/threshold.py` |
| selector | `val_objdice_max`，如 active eval report 另有裁决必须升级版本 | 工程冻结规则 | 固定至版本变更 | `src/eval/checkpoint_selector.py` |
| metrics | Object F1/Dice/Hausdorff + Dice/IoU/HD95/Boundary F1 | 官方协议固定项/工程冻结规则 | 固定 identity | `src/metrics/` |
| Boundary F1 | `project_custom_erosion_xor_3x3_ones_8conn_border0_tol3px` | 工程冻结规则 | 固定 | `src/metrics/seg_metrics.py`、crosscheck |
| N1/N2/N3 | `planned/not_run` | 路线层已锁定 | 禁止提前运行 | `/home/featurize/work/Paper/crc_gland_segmentation_project_journal/configs/stages/05_method_protocol` |
| formal result | `not_run`；资格 `false` | 工程冻结规则 | 固定 | 所有 run_meta/report |

## 规则卡片：评估身份
- 当前结论：primary Object F1 按挑战论文语义；本地 DCAN 风格阈值只作 compatibility audit。
- 规则类型：官方协议固定项 + 工程冻结规则。
- 适用阶段：从 04 评估冻结至 11。
- 直接依据：GlaS、DCAN `metrics.py`、active 04 评估规范。
- 采用原因：避免同名指标实现混写。
- 不采用的相邻方案：不把 compatibility 结果冒充 primary，不用测试集决定 identity。
- 代码落点：`src/metrics/object_metrics.py`、评估配置与 crosscheck report。
- 运行记录字段：`eval_proto_version/object_f1_identity/compatibility_identity/boundary_diff_report`。
- 验收方式：边界样例、实现差异和版本字段均可回查；当前结果仍 `not_run`。
- 核心公式或定义参考：`Dice=2|P∩G|/(|P|+|G|)`，`F1=2PR/(P+R)`，对象匹配按已裁决 overlap 身份。

## 参数变更与运行字段
变更必须记录 `old_value/new_value/reason/evidence/affected_config/protocol_version/approver`。run 还必须记录七字段 lineage、`run_name/status/planned_or_actual/formal_result/seed/config_sha256/split_sha256/checkpoint/metric_identity`。任何参数不在表中即 `blocked`。

## 验收、blind 与 handoff
验收要求 plan/config/run_meta/manifest/Gate/handoff 字段一致；06 只能消费 val；07/08 测试冻结后只读。handoff 必须携带七字段及 `consumption_boundary`，不得传递测试调参权。

## 独立回退条件
出现未登记参数、版本未升级、primary/compatibility 混写、指标 identity 缺差异报告、N1-N3 状态越级、测试泄漏或 formal result 非 `not_run` 时，回退研究定标/评估协议并阻断运行。

## 冲突裁决记录
旧计划参数表中的历史 LKMA/Boundary/Distance 值不作为当前默认；旧工程 selector/metric 只作接口基线。active 评估 identity、journal roots、05-11 route 优先，并将影响同步到 01/03/04/05。

## 文件质量自检
- [x] 参数有当前结论、类型、边界、落点和变更规则。
- [x] 公式/定义、文献和参考代码已回链。
- [x] 固定、可调、禁止、blind、handoff、回退已独立写明。
- [x] 状态为 `planned/not_run`，无实验结果。
- [x] 代码字段与同层评估/命名文件一致。

## 7. 参数消费顺序
数据参数先由 01 数据协议确认，训练参数由 02-04 冻结，方法参数由 05-06 在 val 候选内筛查，测试参数在 07/08 前锁定，汇总只读版本字段。任何跨阶段消费必须携带七字段 lineage。

## 8. 参数验收记录
每个字段需要记录当前值、规则类型、直接依据、候选范围、冻结时点、代码落点、运行字段和回退目标。版本升级必须同时更新 config、run_meta、manifest 和 Gate 输入。

## 9. 当前禁止动作
不得以某次运行数字反推参数，不得将历史参数表当 current，不得在 test 上选择阈值或后处理，不得把 `planned/not_run` 写成 completed。当前不启动任何训练或测试。

## 10. 参数审计清单
- 核对每个固定值的规则类型。
- 核对每个候选值的最终选择和冻结时点。
- 核对每个禁止项有阻断动作。
- 核对代码字段和 config 字段同名。
- 核对 run_meta 和 manifest 同步。
- 核对 selector 与 metric identity 同步。
- 核对 threshold 来源是 val。
- 核对 TestA/TestB/CRAG test 未被筛参。
- 核对版本变更记录完整。
- 核对回退目标明确。
- 核对 formal result 未越级。
- 核对未产生实验数字。

## 11. 当前审计结论
参数总表只作为协议消费，不代表任何参数已通过运行验证；真实 Gate 状态以阶段报告为准。

## 12. 参数审计结论
本表可以阻断未登记参数，但不能替代 stage card、Pre-check、代码质量或运行 Gate。参数冻结的证据是字段一致性和版本同步，不是任何结果数字。

## 13. 参数冲突回流
参数冲突需回到来源文件裁决，随后同步配置、命名、评估和下游 handoff；未同步时保持 blocked。

## 14. 参数使用责任
阶段作者负责消费边界，代码作者负责字段，评估作者负责 identity，治理作者负责版本和 Gate。

## Diagnostics 闭环
整批运行正式文档与 Pre-check 检查，并检查参数表中的旧根、旧阶段名、strict replication、结果状态禁词。发现问题必须回写、复跑并记录真实命令输出。
