# 执行导航

## 1. 文件角色与执行边界
本文件是 active journal 路线 `01_实验执行` 的总入口和阶段门控协议，归属 A 类。它只定义可执行路线、证据边界、交接字段与回退逻辑，不产生实验结果。当前 `status=planning_rewrite_verified_pending_diagnostics`，`formal_result=not_run`，`result_eligibility=false`。

## 2. 本轮直接依赖的前置文件
- 旧计划基线：`/home/featurize/work/Paper/结直肠腺体分割_plan_优化版/01_实验执行/00_总览与规范/00_执行导航.md` 至 `07_实验执行全局修订与质检规范.md`，用于继承完整结构和协议强度；旧根只读。
- active 同层：本目录 `01_工程目录框架.md`、`02_参数冻结总表.md`、`03_命名与结果记录规范.md`、`04_评估口径与官方脚本对齐.md`、`05_代码工程映射与实现策略.md`、`06_实验执行证据化写作模板.md`、`07_实验执行全局修订与质检规范.md`。
- 路线与研究：`02_路线与投稿/01-06`，特别是分阶段路线、研究定标和止损判断。
- 文献与参考资料：GlaS、DCAN、MILD-Net 提取稿；`/home/featurize/work/Paper/结直肠腺体分割_正式参考资料/04_task_specific_benchmarks/GlandSegBenchmarks-master/DCAN/metrics.py`、`/home/featurize/work/Paper/结直肠腺体分割_正式参考资料/04_task_specific_benchmarks/GlandSegBenchmarks-master/DCAN/test_GlaS.py`、surface-distance、MedPy 的说明或源码锚点。
- active 工程：`/home/featurize/work/Paper/crc_gland_segmentation_project_journal` 下 `src/`、`scripts/`、`configs/`、`experiments/`、`reports/`。旧工程入口仅作 historical provenance：`/home/featurize/work/Paper/crc_gland_segmentation_project/scripts/train.py`、`/home/featurize/work/Paper/crc_gland_segmentation_project/scripts/test.py`、`/home/featurize/work/Paper/crc_gland_segmentation_project/src/engine/trainer.py`、`/home/featurize/work/Paper/crc_gland_segmentation_project/src/eval/run_eval.py`、`/home/featurize/work/Paper/crc_gland_segmentation_project/src/eval/checkpoint_selector.py`、`/home/featurize/work/Paper/crc_gland_segmentation_project/scripts/summarize_stage.py`。

## 3. 本阶段唯一允许处理的变量
只处理 active 路线、阶段顺序、交接和门控语义；不在本文件重开模型或实验参数。

## 4. 阶段门控表达式
`Gate_current = governance_valid AND source_sync AND protocol_resolved AND lineage_complete AND formal_result_not_run`。任一项不成立即 `blocked`。

## 上游、同层与下游
上游是 `02_路线与投稿`、`03_文献证据` 和治理记录，提供路线、定义与证据优先级；同层 01-07 将路线翻译为目录、参数、命名、评估、代码、写作和质检；下游为 `01_数据协议`、`02_UNet流程验证`、`03_UNet稳定性`、`04_Baseline`、`05_方法协议`、`06_val筛查`、`07_正式GlaS`、`08_CRAG验证`、`09_外部对比`、`10_结果汇总`、`11_总验收与止损`。下游不得反向改变本文件的 stage order、blind 边界或状态语义。

## 路线依据与文献依据
当前唯一编号路线为 `01 数据协议 -> 02 UNet流程验证 -> 03 UNet稳定性 -> 04 Baseline -> 05 方法协议 -> 06 val筛查 -> 07 正式GlaS -> 08 CRAG验证 -> 09 外部对比 -> 10 结果汇总 -> 11 总验收与止损`。N1/N2/N3 是 05 内的方法身份，不是阶段编号。GlaS 定义 train/val/TestA/TestB 和对象级评价边界；DCAN 支撑 object-contour 联合思想；MILD-Net 支撑同任务双分支与对象级评价。`formal_result` 在所有文档中必须为 `not_run`，不得写入效果、增益或投稿结论。

## 固定、可调与禁止参数
- 固定：active roots、stage order、GlaS/CRAG 身份、train/val/test 盲态边界、七字段 lineage、状态词典、结果资格规则。
- 可调：仅在对应阶段 card 预注册的模型候选、val 阈值、N1-N3 机制候选；任何调整须写 `old_value/new_value/reason/evidence/affected_config/protocol_version/approver`。
- 禁止：读取测试标签调参、把旧 LKMA/Boundary/Distance 当当前路线、将 N1/N2/N3 写成已完成、把历史结果移入 current Gate、启动训练或测试。

## 代码落地接口
导航只规定消费顺序，不实现模型。配置从 `crc_gland_segmentation_project_journal/configs/` 读取；训练、评估、汇总和门控接口分别落在 `scripts/train.py`、`src/eval/run_eval.py`、`src/eval/checkpoint_selector.py`、`scripts/summarize_stage.py` 及对应 `src/engine/trainer.py`。每次 stage handoff 必须同步 `plan/config/run_name/run_meta/manifest/gate/handoff`。

## 运行记录字段与验收
每次计划消费必须记录 `source_stage/source_manifest/source_protocol_version/source_run_name/consumer_stage/consumer_file/consumption_boundary`，以及 `status/planned_or_actual/formal_result`。入口验收要求 active root、manifest 可定位、协议版本一致、blind 边界未破坏、唯一变量已声明；任何字段缺失为 `blocked`，不是 `pass`。

## 独立回退条件
出现旧根路径进入 current 配置、阶段编号冲突、测试集泄漏、状态越级、评估 identity 未裁决、lineage 缺失或结果字段未置 `not_run/false` 时，停止当前工作并回退 `00_计划重建治理`，修订后重新运行 diagnostics。不得以局部文件通过替代全局回退。

## 冲突裁决记录
- 冲突对象：旧路线 `05-07=LKMA/Boundary/Distance` 与 active `05-11`。
- 裁决：active journal `05-11` 优先；旧路线仅 historical provenance。
- 冲突对象：旧工程接口与 journal 新接口。
- 裁决：接口职责可借鉴，路径、状态和结果不迁移；代码只能落 active project root。
- 回流：已同步本目录其余 7 文件，后续阶段仍须继承。

## 文件质量自检
- [x] 文件角色、active roots、A/B/C 与状态语义明确。
- [x] 前置依赖、上游/同层/下游已按导航角色区分。
- [x] 路线、文献、参考代码、固定/可调/禁止项已落正文。
- [x] 代码接口、运行字段、验收、回退、裁决已独立成节。
- [x] 无实验数字；`formal_result=not_run`、`result_eligibility=false`。
- [x] 与同批 `01_工程目录框架.md` 的收尾强度对齐。

## 7. 运行记录字段字典
- `source_stage`：产生协议或资产的阶段。
- `source_manifest`：可定位的 manifest 或 planned manifest 路径。
- `source_protocol_version`：被消费的协议版本。
- `source_run_name`：事实 run 或 `not_applicable_document_only`。
- `consumer_stage`：当前消费者阶段。
- `consumer_file`：当前消费者文件或配置。
- `consumption_boundary`：数据、val、test 或 protocol-only 边界。
- `status`、`planned_or_actual`、`formal_result`、`result_eligibility`：状态事实，不得互换。

## 8. 通过线与止损线
通过线要求前置阅读、路线一致、代码接口、字段链、blind 边界和 diagnostics 全部可回查；止损线是任一项缺失即 blocked。结果未运行时只能写协议、资产计划和验收条件，不能写数字或比较结论。

## 9. 当前禁止动作
本批不启动训练、测试、预测、checkpoint 选择或结果汇总；不修改旧计划、旧工程和历史归档；不使用历史结果填充当前主表；不把治理检查退出 0 写成研究 Gate pass。

## 10. 执行前逐项核对
- 核对 active plan root 与 active project root。
- 核对阶段编号与 N1/N2/N3 身份。
- 核对 val-only screening 与 frozen-test 边界。
- 核对版本字段和七字段 handoff。
- 核对当前结果资格仍为 false。
- 核对历史来源未进入 current Gate。
- 核对路线、文献、代码和治理记录均有正文落点。
- 核对回退目标不是下游结果表。
- 核对文档修改没有触碰训练入口。
- 核对正式参考资料路径可回查。
- 核对检查器命令和输出已记录。
- 核对未启动任何训练。

## 11. 当前审计结论
本文件已完成正文重写，诊断结论以工具实际报告为准；在 formal diagnostics 不是 pass 前，本文件保持 `partial/blocked` 语义。

## 12. 文档交接检查
正文交接时必须保留依据路径、当前状态、消费边界、回退目标和审计命令。下游阶段读取本文件时只允许继承协议，不得从文档推导不存在的结果。任何缺口都必须写入治理记录。

## 13. 最小交接样例
`source_stage=s05; source_manifest=planned; source_protocol_version=p05_v1; source_run_name=not_applicable_document_only; consumer_stage=s06; consumer_file=active stage card; consumption_boundary=val_only_no_test_labels`。
该样例只说明字段形状，不表示运行存在；任何实际值必须来自真实 manifest。

## 14. 复核责任
文档作者负责依据和状态，阶段负责人负责接口和 handoff，治理负责人负责 diagnostics；责任不能由自检勾选替代。

## 导航完整性验收
- 导航只能指向当前 active plan 和 active project；历史路径只能出现在 provenance 说明中。
- 每一阶段必须同时存在阶段协议、实现依据、验收和回退落点；只有入口文件而没有下游验收文件时，阶段状态为 blocked。
- `05_方法协议` 内的 N1/N2/N3 必须通过 method identity、candidate budget、val-only boundary 和 handoff schema 关联，不能被解释为三个编号阶段。
- `06_val筛查` 的候选决策必须能回链到预注册 manifest，`07_正式GlaS` 必须只消费 Gate 通过的候选。
- `10_结果汇总` 只读取正式 raw/aggregate，不生成或补写缺失结果；`11_总验收与止损` 只消费各阶段 Gate 和 handoff。

## Diagnostics 闭环
本轮需运行 `check_formal_stage_docs.py` 和 `check_precheck_docs.py`；工具输出必须回填治理记录。若发现路径、章节、状态或禁词问题，先修正文档再复跑。当前本节只记录协议，不把未运行工具伪装成通过。复核顺序固定为：路径解析 → 阶段顺序 → 状态语义 → blind → lineage → 下游消费边界；任一项失败都回退到治理记录，不允许直接进入实验。
