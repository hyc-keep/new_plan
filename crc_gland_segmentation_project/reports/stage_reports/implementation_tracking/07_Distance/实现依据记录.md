# 07_Distance 实现依据记录

## 1. 编码前状态
- 当前阶段: `07_Distance`
- research_alignment_gate: `pass`
- stage_definition_gate: `pass`
- precheck_doc_gate: `pass`
- 当前主线输入: `boundary_input_base`
- 06 Boundary: `backup`，不默认继承 BoundaryHead
- 本轮目标: 最小实现 `EDT_norm01_v1 + SmoothL1 + lambda_dist=0.1`
- 本轮禁止: 正式训练/测试前不扩展候选 lambda，不改评估协议，不修改 06 历史结果。

## 2. 本轮逐文件计划依据

| 正式文件 | 计划依据 | 参考/官方依据 | 冻结参数与工程决定 |
|---|---|---|---|
| `src/data/distance_targets.py` | 07/02 距离图与 lambda 范围 | SciPy `scipy.ndimage.distance_transform_edt` | binary mask→signed EDT→per-sample norm01；float32；空/全 mask 全零；将原纯 Python EDT 替换为 SciPy 底层实现，数值口径不变。 |
| `src/losses/seg_losses.py` | 07/00 阶段总协议、07/02 | 当前 BCE+Dice builder | 在现有 loss 模块中新增 Distance loss builder；SmoothL1 mean；lambda=0.1。 |
| `src/models/resnet34_unet.py` | 07/00、07/01 设计依据 | 现有 ResNet34-U-Net decoder feature | 新增 distance auxiliary 单通道输出；`use_boundary_head=false` 时不返回 Boundary；保留旧单输出行为。 |
| `src/data/datasets.py` | 07/00、07/02 | 现有 image/mask/sample identity | batch 增加 distance target；保持 image/mask shape 与 dtype 语义。 |
| `src/engine/trainer.py` | 07/03 实验步骤、全局训练冻结表 | 现有 AMP/GradScaler/early stopping | 训练和验证支持 distance target/logit/loss；保留旧单输出和 Boundary 分支。 |
| `src/eval/run_eval.py` | 全局评估口径、eval_proto_v1 | GlaS/DCAN TestA/TestB | segmentation 评估只消费 seg_logits；distance 输出不参与 threshold。 |
| `scripts/train.py` | 07/03 运行步骤 | 现有 config/model/loss factory | 按 `use_distance_head` 选择 D2 loss；正式 D2 config 后续建立。 |
| `scripts/test.py` | 全局评估口径 | 现有 TestA/TestB 入口 | D2 checkpoint 只评估 segmentation logits。 |
| `configs/model/resnet34_unet_distance.yaml` | 07 阶段实现卡、07/02 | 当前 model config schema | 固定 distance_v1、BoundaryHead=false、DistanceHead=true、lambda=0.1。 |
| `configs/experiment/D2_R34UNet_Distance_GlaS_seed3407.yaml` | 07/03 实验步骤、全局 identity 规范 | D1 config schema、eval_proto_v1 | 固定 D2 seed3407 screening、EDT_norm01_v1、SmoothL1、boundary_input_base。 |
| `configs/model/resnet34_unet_distance.yaml` | 07 阶段实现卡、07/02 | 当前 model config schema | 固定 distance_v1、BoundaryHead=false、DistanceHead=true、lambda=0.1。 |
| `b_class_auxiliary/coding_guards/07_Distance/stage_contract.yaml` | 07 阶段实现卡、全局 contract 规范 | 06 stage contract schema | 固定 D2 identity、sample_count、metric_set、lineage、freshness 和 required paths。 |

## 4. 本轮直接依赖的已读文件
- 07_Distance 阶段总协议、设计依据、距离图与 lambda 范围、实验步骤、保留或降级标准。
- 全局参数冻结表、评估口径与官方脚本对齐、代码工程映射与实现策略。
- 06 Boundary decision note 和 workflow gate。
- `src/data/distance_targets.py`、`src/data/datasets.py`、`src/losses/seg_losses.py`、`src/models/resnet34_unet.py`、`src/engine/trainer.py`、`src/eval/run_eval.py`、`scripts/train.py`、`scripts/test.py`。
- 正式参考资料中的 Distance/Shape 文献证据和 GlaS/DCAN metrics/test 参考代码。

## 5. 正式文件到依据的回链
- 以上每个正式 py/yaml 文件均已在本记录第 2 节逐文件列出，并回链到 07 协议、全局评估协议和工程接口。
- 正式配置与 stage contract 已单独登记，D2 identity、seed3407、lineage、schema 和 freshness 已锁定。

## 6. 参数与规则冻结回填
- distance_target_version: `EDT_norm01_v1`
- distance_loss: `SmoothL1`
- lambda_dist: `0.1`
- empty_mask_target/full_mask_target: `zeros`
- use_boundary_head: `false`
- use_distance_head: `true`
- distance logits 不参与 segmentation threshold；正式评估继续使用 `eval_proto_v1`、val_objdice_max、threshold=0.5、float32 和 TestA/TestB split。

## 3. 实际编码完成后回填
- 实际修改文件与函数：待编码完成后逐项回填。
- py_compile 命令与结果：待真实运行后回填。
- target smoke：待真实运行后回填。
- runtime 三件套：`b_class_auxiliary/coding_guards/07_Distance/runtime_check_report.md`、`runtime_evidence.json`、`runtime_check.log` 已真实生成并通过。
- code quality：待 Post-QC Guard 建立后真实运行回填。

## 4. 诚实边界
- 本记录不是正式训练结果，也不代表 Distance 科学效果成立。
- SmoothL1、空/全 mask 全零和 decoder auxiliary head 是工程收敛决定，不是论文直接最优结论。
- 只有 runtime、smoke、正式训练、TestA/TestB、独立复核和 workflow gate 全部完成后，才允许裁决 Distance keep/backup/drop。
