# 07_数据阶段验收

## 1. 本阶段最小交付物（交付物）
数据阶段计划交付 source/split/check/label/preview/manifest/config 资产及 Gate/handoff 报告；当前均不得视为已交付。

## 2. 核心验收问题（验收问题）
验收必须回答资产是否存在、schema/hash是否一致、lineage和blind是否完整、下游是否可安全消费。

## 4. 红线条件
旧根 current 引用、跨 split 泄漏、错配、标签漂移、manifest缺失、测试回流和状态越级均为红线。

## 结论
当前结论为 `planned/blocked`，没有实际数据通过或结果资格。

## 5. 阶段通过声明
本阶段尚未执行真实数据验收；`formal_result=not_run`、`result_eligibility=false`，因此不能声明数据阶段通过或向 02 交付 accepted handoff。

## 文档身份与当前状态
A 类数据阶段 Gate 协议。`status=planned`、`data_stage_gate=blocked`、`formal_result=not_run`、`result_eligibility=false`。本文件只定义验收规则，不伪造数据已通过。

## 本轮重写直接依赖的前置文件
active 00-07规范；数据协议00-06；路线02-03与最低证据包；文献 GlaS Challenge、MILD-Net、TA-Net；active project 实际目录、接口、现存 CSV/运行资产。旧计划/旧工程只在 provenance/冲突裁决中出现。

## 1. 文件角色与执行边界（角色边界）
本文件只聚合数据阶段 Gate、红线、回退和下游准入；不生成数据、不启动训练、不改写历史结果。

## 2. 本轮直接依赖的前置文件（前置依赖）
前置为 active 00-07、数据协议00-06、路线/文献核心文件和 active project 实际资产状态。

## 3. 本阶段唯一允许处理的变量（固定参数与变量边界）
固定七条数据链、blind、lineage和结果状态；可调仅报告版本和预注册抽查清单，禁止手工改 Gate。

## 4. 阶段门控表达式（数据阶段门控/通过线）
全部子 Gate、manifest、schema/hash和 preflight 有真实证据且无红线才可 accepted；当前 planned/blocked。

## 上游、同层与下游
- 上游：01数据源、02检查、03/04 split、05标签预览、06产物清单；本文件只聚合其事实，不重写规则。
- 同层：00阶段总协议定义总顺序；本文件是数据阶段最终验收与回退节点。
- 下游：02 UNet流程验证以及后续03-11；没有本文件真实 handoff 不得消费数据。

## 路线依据
当前唯一路线为 `01 数据协议 -> 02 UNet流程验证 -> 03 UNet稳定性 -> 04 Baseline -> 05 方法协议 -> 06 val筛查 -> 07 正式GlaS -> 08 CRAG验证 -> 09 外部对比 -> 10 结果汇总 -> 11 总验收与止损`。N1/N2/N3属于05。旧 LKMA/Boundary/Distance只作 historical provenance。

## 文献依据
GlaS定义官方 train85/TestA60/TestB20与对象级任务边界；MILD-Net/TA-Net支持 CRAG第二 benchmark。文献结果不构成 current data Gate。

## 固定、可调、禁止参数
- 固定：GlaS 68/17/60/20、seed3407；CRAG 153/20/40、`train_sup16 union train_unsup137`、local provenance；mask>0；test blind；七字段 lineage。
- 可调：仅验收报告版本、证据路径和已预注册抽查清单；不得改 split、标签、消费边界。
- 禁止：把检查器通过当研究 Gate pass、以历史运行资产替代数据验收、测试反馈选择、手工写 `data_stage_pass=true`、启动实验。

## 验收聚合与输入/输出 schema
规则表达式：`data_stage_gate = source_ok AND split_ok AND pair_ok AND label_ok AND check_ok AND preview_ok AND manifest_ok AND lineage_complete AND blind_preserved`。每个子项必须引用对应 asset manifest、报告路径、schema和sha256。输出 planned artifact directory reports/gates；planned_not_created:reports/gates/data_stage_acceptance.json 和 planned_not_created:reports/gates/data_stage_acceptance.md，字段至少 `protocol_version,status,subgates,red_flags,asset_manifest_sha256,lineage,formal_result,result_eligibility,next_action`。当前输出为 `planned_not_created`。

验收对象：source manifest；GlaS四CSV和 CRAG三CSV；check报告；mask/preview资产；config；总manifest。下游消费边界：只有 status=accepted 的真实 Gate 才能进入02；当前不存在 accepted事实，因此下游 blocked。

## 代码落地接口
- current/existing：`/home/featurize/work/Paper/crc_gland_segmentation_project_journal/b_class_auxiliary/tools/check_formal_stage_docs.py`、`check_precheck_docs.py`可做文档检查；active `src/data`/`src/eval`为潜在消费者。
- planned_not_created：scripts/run_data_gate.py、scripts/validate_handoff.py、reports/gates/data_stage_acceptance.json/md；目录契约为 reports/gates/。
- historical provenance：旧工程验收脚本/报告不可作为 current data Gate。

## 运行记录字段与 lineage
必须记录 `source_stage/source_manifest/source_protocol_version/source_run_name/consumer_stage/consumer_file/consumption_boundary` 及 `data_protocol_version,status,subgates,red_flags,asset_manifest,asset_manifest_sha256,source_digest,split_sha256,label_digest,preview_digest,preflight_status,planned_or_actual,formal_result,result_eligibility,next_action`。当前：`source_stage=s01_data_protocol; source_manifest=planned_not_created; source_protocol_version=data_protocol_v1; source_run_name=not_run; consumer_stage=s02_unet_flow_validation; consumer_file=planned_not_created; consumption_boundary=train_val_only_no_test_labels`。

## 验收规则
- 通过规则：所有子 Gate有真实报告、资产可回指、schema/hash一致、blind边界保留、无红线。
- 当前状态：`planned/blocked`，不能写实际 pass；`formal_result=not_run`、`result_eligibility=false`。
- 文件存在、旧报告、历史 checkpoint、checker退出0都不是数据验收事实。必须重新生成本协议版本对应的资产和报告。

## 独立回退条件
按顺序回退：旧根/来源 -> 配对检查 -> GlaS/CRAG split -> 标签/预览 -> 资产 manifest -> Gate/handoff。触发条件包括任何红线、缺字段、hash冲突、测试泄漏、状态越级、source provenance误写、current接口未接入。回退期间禁止进入02和任何实验。

## 冲突裁决记录
- active root和01-11 route优先；旧计划/旧工程只作 provenance。
- CRAG local split不宣称 universal official。
- current project已有运行资产不代表本数据阶段通过，历史/现存资产与 planned协议分离。
- 当前 Gate 只能 planned/blocked；不得将文档诊断通过改写为数据阶段 pass。

## 实施与状态转移
- 验收输入按 source、pair/check、GlaS split、CRAG split、label/preview、manifest/config 分项登记；缺项写 `planned_not_created`，不填默认通过。
- 每个子 Gate 记录 `status,observed_value,expected_rule,evidence_path,evidence_sha256,owner,next_action`；证据不可解析时状态为 blocked。
- 总状态只允许 `planned/blocked/partial/pass`；本轮是 `planned/blocked`，文档 checker 的 pass 不能改变 data_stage_gate。
- 只有 accepted handoff 才能交给 02；02-04 使用 train/val，06 只选择 val，07/08 使用盲态测试，任何越界都触发红线回退。
- 回退必须保留原 Gate、触发字段和 digest，修复后升级 protocol_version 并重新生成完整子 Gate，不在原报告上覆盖。

## 字段级验收清单
- 核对 source、pair/check、GlaS split、CRAG split、label/preview、manifest/config 的子 Gate 均有 evidence_path 和 evidence_sha256。
- 核对 GlaS train68/val17/TestA60/TestB20、seed3407、CRAG train153/val20/test40 和 blind 语义均来自当前协议。
- 核对每个子 Gate 的 `observed_value,expected_rule,status,next_action` 完整，缺证据不得默认为 pass。
- 核对七字段 lineage、CSV schema、mask>0、manifest/hash 和 preflight 绑定同一 protocol_version。
- 核对 02 只接收 accepted handoff；文档 checker pass、文件存在和历史结果都不能替代 data_stage_gate。
- 核对 selector、threshold、postprocess 仅由 val 产生，TestA/TestB/CRAG test 不回流。
- 核对任何红线都保留原 Gate 和 digest，回退后升级协议版本并重新验收。
- 核对当前结论严格为 planned/blocked、formal_result=not_run、result_eligibility=false。

## 文件质量自检
- [x] 验收聚合、红线、资产 schema、消费边界和回退顺序完整。
- [x] current/existing/planned_not_created、代码接口、lineage和结果状态明确。
- [x] 独立前置、上游/同层/下游、路线、文献、冲突、Diagnostics和审计对表均存在。
- [x] 未写真实数据通过、性能数字或实验结论。
- [ ] 本轮未运行数据 preflight/Gate，仍为 blocked。

## Diagnostics 闭环
本轮已实际完成 active规范、路线/文献、旧版八文件和 active project状态核对；发现完整 source/check/label/preview/manifest/config/handoff尚未形成可证明闭环，已将当前 Gate 明确保持 `planned/blocked`。对照模板为 active `00_执行导航.md`、`01_工程目录框架.md`及同层 `06_数据产物清单.md`，收尾字段和审计粒度已对齐。未启动训练、测试、预测或结果汇总。

## 审计对表
| 已读文件 | 正文落点 | 自检落点 | diagnostics | 当前缺口 | 修复状态 | 对照模板 | 补齐项 |
|---|---|---|---|---|---|---|---|
| active 00-07 | 前置、聚合、字段、回退 | 自检1-5 | 已静态核对 | data Gate未运行 | 已修状态语义 | active 00/01 | 保留 blocked |
| 同层数据协议00-06 | 上游/同层、验收对象 | 自检1-3 | 已互校 | 资产未闭环 | 已补消费边界 | active 06 | 增加红线回退 |
| 路线/文献 | 路线依据、文献依据 | 自检1-2 | 已核对 | 不产生结果 | 已分离 quoted/current | active 00 | 无 |
| active project | 代码接口、Diagnostics | 自检2/5 | 已检查存在/缺失 | gate脚本未生成数据Gate | 已标 planned | active 01 | 无 |
