# 01-03 阶段整改与 04_Baseline 放行执行协议

> 建立日期：2026-07-14  
> 最近同步：2026-07-15  
> 文件性质：01-03 阶段整改、证据链同步、04_Baseline 进入判据。  
> 当前结论：02/A1 与 03/A2 的当前实验资产和专属 workflow gate 已通过；允许进入 04 的研究、阶段锁定和 Pre-check，不得把 02/03 的通过结论当作 04 自身通过。

## 1. 当前正式身份与结果来源

- A1：`A1_UNet_GlaS_v1_seed3407`
- A2：`A2_UNet_GlaS_seed3407`、`A2_UNet_GlaS_seed1234`、`A2_UNet_GlaS_seed2025`
- 当前评估协议：`eval_proto_v1`
- `A2_UNet_GlaS_v1_seed*` 仅是配置文件路径版本名，不是当前 A2 run_name。
- `protocol_v3`、旧高 F1 数字和旧 aggregate 资产只保留历史追溯，不能进入当前 A2 聚合或 04 handoff。
- 当前主结果唯一来源：`reports/tables/unet_mean_std_summary.csv`。

## 2. 已完成的真实整改与证据

| 项目 | 当前状态 | 证据 |
|---|---|---|
| A1 正式评估 | 通过 | A1 TestA=60、TestB=20；GPU 评估成功 |
| A2 三 seed 评估 | 通过 | 三个当前正式 seed 均完成 TestA/TestB GPU 评估 |
| 同空间评估资产 | 通过 | 每个 run 均生成 eval image、GT、prediction PNG；尺寸不一致硬失败 |
| 测试 CSV | 通过 | 当前正式 CSV 为 sample-only；测试 loss/loss_bce/loss_dice 明确为 `not_applicable`；包含 source/eval lineage |
| 独立指标复核 | 通过 | A1 与 A2 三 seed 的 TestA/TestB 均 `status=pass`；missing=0、mismatch=0、最大绝对误差=0；指标复核不等于所有 worst-case overlay 都已导出 |
| 指标集合 | 通过 | F1、Object Dice、Object Hausdorff、Dice、IoU、HD95、Boundary F1 均已进入当前 raw/mean±std 表 |
| A2 聚合 | 通过 | raw seed 表 42 行；mean±std 表 14 行；`n_runs=3`；`std=population std (ddof=0)` |
| A1 summary/manifest | 通过 | `stage_pass=true`、`protocol_error=false`、`next_action=enter_03_unet_stability` |
| A2 summary/manifest | 通过 | `stage_pass_a2=true`、`handoff_ready_for_b1=true` |
| 专属 workflow gate | 通过 | 02 与 03 的 research、stage definition、pre-check、runtime、code-quality、workflow 报告均为 pass |
| checkpoint 可加载/forward | 通过 | A1/A2 checkpoint 可加载并完成 forward 核验；无需全部重训 |
| 严格 checkpoint identity 绑定 | 通过 | 三个正式 A2 run 已使用现有 best.ckpt 完成真实 test.py 重测，checkpoint SHA256 写入 run_meta，identity status=pass |

## 3. 当前正式指标（A2 mean±std）

| split | F1 | Object Dice | Dice | IoU | HD95 | Boundary F1 | Object Hausdorff |
|---|---:|---:|---:|---:|---:|---:|---:|
| testA | 0.5291±0.0653 | 0.7081±0.0529 | 0.8687±0.0142 | 0.7803±0.0232 | 57.4652±3.8713 | 0.6283±0.0388 | 128.8409±28.7017 |
| testB | 0.5865±0.0177 | 0.7756±0.0121 | 0.8785±0.0080 | 0.7926±0.0095 | 47.2812±10.7473 | 0.6155±0.0026 | 125.0122±10.7744 |

解释边界：F1 是 GlaS 对象级检测 F1，不是像素 Dice；n=3 的 std 只描述三 seed 内部离散度，不是统计显著性或置信区间。

## 4. 本轮同步修补的文件

本轮发现并修补了以下说明文档与实现依据中的不同步内容：

1. `reports/stage_reports/implementation_tracking/03_UNet稳定性/README.md`
   - 将 per-run 评估资产改为 sample-only CSV、同空间 PNG、独立 PNG+GT 复核。
   - 将证据链写明为 run_meta → per-run CSV/PNG → 独立复核 → raw/mean±std 汇总。
   - 修正 A2 gate 描述，加入 `blockers_resolved`。
   - 明确 10+ 篇说明文只代表当前 A 类对象清单，不代表所有历史/辅助文件都可当前消费。
2. `reports/stage_reports/implementation_tracking/03_UNet稳定性/scripts_test.py.md`
   - 修正测试 CSV 不写 aggregate 行的描述。
   - 补充同空间 eval image/GT/prediction、source/eval lineage 和 `not_applicable` loss。
   - 补充独立 PNG+GT 复核和 shape/missing 失败条件。
3. `reports/stage_reports/implementation_tracking/03_UNet稳定性/scripts_summarize_stage.py.md`
   - 修正 raw 表来源为 sample-only CSV 重聚合。
   - 修正当前汇总口径为 raw per-seed=42 行、mean±std summary=14 行；per-run TestA/TestB CSV 均为 sample-only，不含 aggregate 行。
   - 修正结果路径和验证标准。
4. `reports/stage_reports/implementation_tracking/03_UNet稳定性/reports_tables_unet_seed_results.csv.md`
   - 修正指标数为 7、数据行数为 42、总行数为 43。
   - 修正 per-run 对账方式，不再引用不存在的 aggregate 行。
5. `reports/stage_reports/implementation_tracking/03_UNet稳定性/reports_tables_unet_mean_std_summary.csv.md`
   - 修正聚合来源和 14 行统计口径。
6. `reports/stage_reports/implementation_tracking/03_UNet稳定性/implementation_status.md`
   - 修正 raw=42、mean±std=14。
7. `reports/stage_reports/implementation_tracking/03_UNet稳定性/00_交付范围内正式对象清单.md`
   - 区分汇总层正式表与逐 run 过程证据。
8. `reports/stage_reports/implementation_tracking/03_UNet稳定性/实现依据记录.md`
   - 修正旧的“Dice==F1”实现说明，明确当前 F1 为对象级、Dice 为像素级。

## 5. 全量审计裁决与文件边界

本轮已覆盖工程 Markdown、02/03 implementation_tracking、02/03 计划正文、notes、reports、b_class_auxiliary、experiments 当前目录与 `_historical_archive`，以及 `/home/featurize/work/Paper/乱` 下的审查/交接文档。审计按“当前消费文件、历史 provenance、治理/辅助文件”三类处理，不把历史数字机械改写成当前结果。

### 5.1 已修补的当前消费文件

- 02/A1 implementation_tracking：测试 CSV、crosscheck、视觉导出、run_meta、run_summary、best/last checkpoint、train/val log、train/val curve；已同步 GPU 正式 run、60/20、sample-only、同空间资产、独立复核和当前 50/70 epoch 事实。
- 根目录 `debug-a1-train-stop.md` 已关闭并标记为历史调试记录；`notes/debug_note.md` 已标记为过程 provenance，不承担当前 A1 gate。当前 A1 summary 已补齐 50/70、CUDA、AMP 和 best selector。
- 03/A2 implementation_tracking：README、测试/聚合说明、raw/mean±std 表说明、对象清单、实现依据、运行说明；已同步三个正式 run、七项指标、raw=42、mean±std=14、sample-only 和当前 gate。
- 03 阶段锁定辅助文件：`b_class_auxiliary/coding_guards/20260708_03_unet_stability_stage_lock/实现依据记录.md`、`pre_check_extraction.md`；已区分配置路径 `A2_UNet_GlaS_v1_seed*.yaml` 与正式 run_name `A2_UNet_GlaS_seed*`，并写明当前 CSV/汇总契约。

### 5.2 保留但已隔离的历史文件

以下文件保留原始旧数字和过程记录，不得作为当前入口；已补充 `historical_archive_only=true`、`valid_for_current_gate=false` 标识：

- `/home/featurize/work/Paper/乱/03_UNet稳定性_最终审查报告.md`
- `/home/featurize/work/Paper/乱/03_UNet稳定性_审查交接与问题记录.md`
- `/home/featurize/work/Paper/乱/03_UNet稳定性_交接说明.md`
- `/home/featurize/work/Paper/乱/03阶段审查报告.md`
- `/home/featurize/work/Paper/乱/02_UNet流程验证_严格修补方案.md`
- `/home/featurize/work/Paper/乱/02_UNet流程验证_执行检查清单.md`
- `/home/featurize/work/Paper/乱/服务器切换接力说明.md`

其他已明确带历史标识的 `notes/04_*`、`a2_v3_blockers.md`、旧 protocol_v3 和 `_historical_archive` 文件继续保留 provenance，不进入当前 gate。

### 5.3 当前计划正文的处理规则

02/03 计划正文中出现的历史身份映射、旧协议和冻结阶段记录属于计划 lineage；已明确标注历史的内容不机械替换。当前入口只消费当前实现跟踪、当前 run_meta、当前 raw/mean±std、当前 stage summary 和当前 workflow gate。

判断原则：历史文件不删除、不改写成当前结果；当前/未来消费文件必须与真实代码、CSV、PNG、manifest 和 gate 同步。

## 6. 治理闭环状态

```text
治理/技能规则              已审计
研究定标                    02 pass；03 pass
阶段锁定                    02 pass；03 pass
Pre-check                   02 pass；03 pass
编码质量                    02 pass；03 pass
运行证据                    A1/A2 当前资产已真实完成
代码质量                    02 pass；03 pass
总 workflow gate            02 pass；03 pass
说明文档                    本轮已修补一批；仍需继续清理历史说明边界
交付前清理                  02/03 已完成当前资产边界；04 尚未开始
```

共享 `b_class_auxiliary/runtime_checks/post_qc_guard.md` 是治理公共输入；02/03 专属 workflow 报告已分别按各自 research record、stage card、Pre-check Guard 和 experiment config 重新生成。共享历史报告不改写，当前裁决以专属当前报告和真实资产为准。

## 7. 放行裁决

- 不需要全部重训。
- 不需要全部推倒重来。
- A1/A2 当前实验指标已通过资产一致性和独立 PNG+GT 复核，可以作为当前阶段正式结果和后续基线输入；A2 metric gate、严格 checkpoint identity、Pre-check、code-quality、workflow gate 均已通过，seed2025/TestA/sample45 overlay 已重新导出；04 自身尚未通过，下一步只能进入 04 research/stage-lock。
- 可以进入 04_Baseline 的研究、阶段锁定和 Pre-check。
- 不得跳过 04 自身 runtime、code-quality、workflow、说明文档和交付前清理。
- 论文可以使用上述当前 A2 数值，但必须明确 split、指标层级、评估协议、mean±std 计算方式和 n=3 的解释边界。

## 8. 全量审计对表

| 审计范围 | 当前证据落点 | 历史/共享边界 | Diagnostics | 修复状态 |
|---|---|---|---|---|
| 02/A1 当前消费说明 | implementation_tracking/02、A1 run_meta、unet_flow_stage_summary | 旧 48/68、CPU、debug 已标历史 | 关键字复扫无未标当前冲突；目标 Markdown diagnostics 无问题 | 已修补 |
| 03/A2 当前消费说明 | implementation_tracking/03、A2 run_meta、raw=42、mean±std=14、stage summary | 旧 v1/protocol_v3 已在计划 lineage 或历史文件中隔离 | 关键字复扫无未标旧行数/旧 gate；目标 Markdown diagnostics 无问题 | 已修补 |
| 03 阶段锁定辅助 | coding_guards/20260708_03.../00_阶段实现卡.md、stage_gate_check.md、实现依据记录.md、pre_check_extraction.md | 配置路径 v1 与正式 run_name 已分开；七项指标、raw42、sample-only/no-aggregate 已写明 | 目标 Markdown diagnostics 无问题 | 已修补 |
| 共享 runtime | runtime_check_report.md、post_qc_guard.md | `stage_code=A2`、`valid_for_04_gate=false` | 共享路径仍是阶段作用域，不得被 04 默认消费 | 已隔离 |
| 旧 A2/B1 reports | unet_v3_*、baseline*_stage_summary.md | `historical_archive_only=true`、`valid_for_current_gate=false` | 旧数字保留但不可当前消费 | 已隔离 |
| 当前 04 workflow | coding_guards/04_Baseline/workflow_gate_report.md | 共享 workflow report 仅为 route probe | 当前专属产物缺失，状态 blocked | 未放行，属于 04 阶段阻塞 |
| 根目录调试/数据审查 | debug-a1-train-stop.md、notes/debug_note.md、reports/data_checks | 调试记录已关闭；数据审查链接需在消费前确认路径有效 | 无代码 diagnostics；路径真实性属于资产复核项 | A1 调试已隔离，数据审查保留待 04 消费时复核 |

## 9. 审计对表与复核要求

| 审计对象 | 正文/代码证据 | 当前状态 |
|---|---|---|
| 指标实现 | `src/metrics/seg_metrics.py`、`src/eval/run_eval.py` | F1 与 Dice 已分离 |
| 阈值 dtype | `src/eval/threshold.py` | float32 before threshold |
| 评估资产 | `scripts/test.py`、各 run eval_assets/predictions | 同空间、无缺失、无 shape mismatch |
| 独立复核 | `notes/independent_metric_check_*.json` | 四个正式 run 全 pass |
| 聚合 | `scripts/summarize_stage.py`、raw/mean±std CSV | 42/14，协议一致 |
| 02/03 workflow | 专属 coding_guards 报告 | 当前报告 pass |
| 文档同步 | 本协议第 4、5 节 | 已修补部分；历史说明仍需分批收口 |

## 11. 当前分数

02/03 当前整改和真实证据闭环：**A1/A2 当前实验与 03 workflow gate 已通过**。当前只保留一个编号阶段边界：04/Baseline 尚未开始；该结论不是模型性能分数，也不是 04 正式 gate 分数。

## 12. 复盘：本轮反复修补的根因分层

二、本轮审计发现的硬阻塞项与处理结果

本节记录本轮用户复核提出的 P0/P1 项。已完成项不再作为当前阻塞；后续 04 只消费当前总 handoff manifest 和当前 gate。

- Pre-check：pass。
- Code quality：pass。
- Workflow：pass。
- 02 六篇计划和 03 四篇计划 diagnostics 无问题。
- 02/03 当前入口已补齐七字段 lineage。
- 03 tracking 未勾选项已收口。
- A2 实验结果文件没有被本轮文档治理修改。

| 层级 | 主要问题 | 严重性 | 是否需要重训 |
|---|---|---:|---|
| 代码/输出契约 | `test.py` 未自动把 checkpoint SHA256 与 identity status 写入 run_meta；`summarize_stage.py` 的 A2 gate 仍使用旧 `aggregated_results_ready` 语义 | 最高 | 不需要，修代码后用已有 checkpoint 重新测试/汇总 |
| 计划/配置身份 | 配置路径 `A2_UNet_GlaS_v1_seed*.yaml` 与正式 run_name `A2_UNet_GlaS_seed*` 混用；多 seed 曾用压缩路径表达 | 高 | 不需要，修契约和 Pre-check 路径后重跑门禁 |
| 统计 schema | sample-only raw、derived aggregate、mean±std 行数和 Gate 条件未一次性锁定 | 高 | 不需要，重新生成汇总表即可 |
| 门禁/治理 | 共享 runtime/workflow 入口承载阶段专属状态；Pre-check 路径无法解析；旧 blocker 未随真实重测同步 | 中高 | 不需要，重新生成对应 gate 和状态记录 |
| 说明文档 | README、summary、crosscheck、B1 blocker 与当前真实状态不同步 | 中 | 不需要，只能在真实产物稳定后同步 |
| 模型性能 | A2 TestA 对象级 F1 波动、Object Hausdorff 较高、存在粘连/边界/小腺体失败 | 基线边界 | 不因该问题回退；作为后续方法改进对象 |

**复盘结论：** 本轮最严重的不是训练失败，也不是指标 CSV 本身错误，而是“运行前契约没有被代码和门禁强制执行”。当前 P0/P1 整改和 02/03 最终门禁已重新通过；后续阶段必须先通过总入口新增的运行前契约清单，再开始正式训练/测试；测试通过后只允许由真实产物驱动文档和 gate 同步。

## 13. 下一步执行顺序

1. 04 开始前必须重新验证 `reports/data_checks` 中历史 Windows 导出链接在当前 Linux 工作区的可访问性。
2. 04 必须先补齐 `coding_guards/04_Baseline/` 专属 research、stage definition、Pre-check、runtime、code-quality 和 workflow 产物。
3. 04 consumer 只能读取专属 current 文件，不能扫描共享 runtime 或旧 summary 文件名。
4. 对本协议和所有修补文件重新读取、运行 diagnostics，并记录缺口；不直接编码或宣称 04 通过。

> 本协议完成不等于阶段自动通过；真实实验资产、独立复核、专属 gate 和文档同步必须同时成立。
