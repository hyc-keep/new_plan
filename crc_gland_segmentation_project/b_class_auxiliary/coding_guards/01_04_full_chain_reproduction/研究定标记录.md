# 01-04 全链研究定标记录

- 状态：`pre-run only / pending_not_run`。
- 研究结论状态：`blocked_pending_pre_run_only`；本记录不表示训练、测试、指标或 Gate 通过。
- 任务边界：建立一条从 01 数据冻结到 04 B1 的独立准备链，历史 01-04 资产仅用于审计边界，不作为新链输入或证据。

## 已读依据

- `configs/data/glas.yaml`：固定 GlaS 数据根、四个 split、数据协议版本与预处理参数。
- `scripts/train.py`：experiment YAML 解引用 data/model/train/eval/asset_manifest，训练前读取 formal handoff；本轮只据此校验配置兼容性。
- `experiments/reproducibility_audit/01_data_protocol/data_protocol_hashes.yaml`：提供已审计的四项当前哈希基线。
- `notes/04_Baseline_A2结果身份审计记录_20260712.md` 与本次审计事实：02 A1 lineage 未闭合；03 A2 与 04 重用且 aggregate/sample CSV 不一致。
- `reports/stage_reports/implementation_tracking/项目问题与决策登记.md`：历史/当前资产的不可混用与 future round 不得继承结论边界。

## 锁定约束

1. 01 由工具实际重算四 hash，并写入根路径无关的 freeze manifest。
2. 02 只准备一个新 A1 seed 3407；03 只准备新 A2 三 seed；04 只准备新 B1 三 seed。
3. 04 只能消费新 03 A2 manifest，明确禁止重新跑 A2，禁止引用任何历史 A2/B1。
4. 新 run 名包含 `chain` 身份词但不包含 round id 或 `v` 序号；round id 只存在于主 contract 审计字段。
5. 当前没有训练、测试、runtime、smoke、checkpoint、指标或汇总运行证据。任何未来数值都未填入本轮文件。

## 工程与论文边界

使用独立 manifest、配置路径、输出目录及 `chain` 命名是工程收敛决定，不是论文结论。模型、数据、训练和评估引用继续采用现有可靠 v1 配置；本记录没有推导或声称任何模型性能。
